
# GENERATED BY ARCHIE — entity: veritasnetbackupsaas
# solution_id: 5373
# bundle_id: code_2026-04-06T18-28-31Z
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

from __future__ import annotations

import uuid
import logging
from http import HTTPStatus

from flask import Blueprint, jsonify, request
from flask_jwt_extended import jwt_required, get_jwt_identity
from marshmallow import ValidationError
from sqlalchemy import or_

from database import db
from app.models.veritasnetbackupsaas import Veritasnetbackupsaas
from app.schemas.veritasnetbackupsaas_schema import (
    VeritasnetbackupsaasSchema,
    VeritasnetbackupsaasCreateSchema,
    VeritasnetbackupsaasUpdateSchema,
)

logger = logging.getLogger(__name__)

veritasnetbackupsaas_bp = Blueprint(
    "veritasnetbackupsaas",
    __name__,
)

_schema = VeritasnetbackupsaasSchema()
_schema_many = VeritasnetbackupsaasSchema(many=True)
_create_schema = VeritasnetbackupsaasCreateSchema()
_update_schema = VeritasnetbackupsaasUpdateSchema()

# ---------------------------------------------------------------------------
# GET /  — list with pagination + optional search
# ---------------------------------------------------------------------------

@veritasnetbackupsaas_bp.route("/", methods=["GET"])
@jwt_required()
def list_veritasnetbackupsaass():
    """Return a paginated list of veritasnetbackupsaas records."""
    page = request.args.get("page", 1, type=int)
    per_page = min(request.args.get("per_page", 20, type=int), 100)
    search = request.args.get("search", "", type=str).strip()

    query = Veritasnetbackupsaas.query

    paginated = query.order_by(Veritasnetbackupsaas.created_at.desc()).paginate(
        page=page, per_page=per_page, error_out=False
    )
    return jsonify(
        {
            "items": _schema_many.dump(paginated.items),
            "total": paginated.total,
            "page": paginated.page,
            "pages": paginated.pages,
            "per_page": paginated.per_page,
        }
    )

# ---------------------------------------------------------------------------
# GET /<id>  — single record
# ---------------------------------------------------------------------------

@veritasnetbackupsaas_bp.route("/<string:item_id>", methods=["GET"])
@jwt_required()
def get_veritasnetbackupsaas(item_id: str):
    item = db.session.get(Veritasnetbackupsaas, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# POST /  — create
# ---------------------------------------------------------------------------

@veritasnetbackupsaas_bp.route("/", methods=["POST"])
@jwt_required()
def create_veritasnetbackupsaas():
    try:
        data = _create_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    item = Veritasnetbackupsaas(
        id=str(uuid.uuid4()),

        **{k: v for k, v in data.items() if v is not None},
    )
    db.session.add(item)
    db.session.commit()
    logger.info("Created veritasnetbackupsaas id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item)), HTTPStatus.CREATED

# ---------------------------------------------------------------------------
# PUT /<id>  — full update
# ---------------------------------------------------------------------------

@veritasnetbackupsaas_bp.route("/<string:item_id>", methods=["PUT"])
@jwt_required()
def update_veritasnetbackupsaas(item_id: str):
    item = db.session.get(Veritasnetbackupsaas, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND

    try:
        data = _update_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    for key, value in data.items():
        if value is not None:
            setattr(item, key, value)

    db.session.commit()
    logger.info("Updated veritasnetbackupsaas id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# DELETE /<id>
# ---------------------------------------------------------------------------

@veritasnetbackupsaas_bp.route("/<string:item_id>", methods=["DELETE"])
@jwt_required()
def delete_veritasnetbackupsaas(item_id: str):
    item = db.session.get(Veritasnetbackupsaas, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    db.session.delete(item)
    db.session.commit()
    logger.info("Deleted veritasnetbackupsaas id=%s user=%s", item_id, get_jwt_identity())
    return "", HTTPStatus.NO_CONTENT
