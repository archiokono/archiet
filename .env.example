



# archiet-ai-native-architecture-to-code-saas-platform — Environment Configuration
# Generated by A.R.C.H.I.E. from Solution #5373
# Bundle: code_2026-04-06T18-28-31Z
#
# Copy to .env and fill in values before running bootstrap.sh or make dev.

# ── Application ────────────────────────────────────────────────────────────
ENV=development
API_PORT=5000
FRONTEND_PORT=3000
LOG_LEVEL=info

# ── Security ───────────────────────────────────────────────────────────────
# Generated automatically by bootstrap.sh — DO NOT commit real values
SECRET_KEY=CHANGE_THIS_IMMEDIATELY
JWT_SECRET=CHANGE_THIS_IMMEDIATELY
ADMIN_EMAIL=admin@archiet-ai-native-architecture-to-code-saas-platform.internal
ADMIN_PASSWORD=CHANGE_THIS_IMMEDIATELY

# CORS: comma-separated list of allowed origins (no wildcards in production)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Maximum items returned per page on list endpoints
MAX_PAGE_SIZE=200

# ── Database ───────────────────────────────────────────────────────────────
# Used by docker-compose — change POSTGRES_PASSWORD before deploying
POSTGRES_DB=archiet_ai_native_architecture_to_code_saas_platform
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme  # secrets-safety-ok — template placeholder for generated .env.example

# Direct URL for local development outside Docker
DATABASE_URL=postgresql+asyncpg://app:changeme@localhost:5432/archiet_ai_native_architecture_to_code_saas_platform  # secrets-safety-ok


# ── Auth (JWT) ─────────────────────────────────────────────────────────────
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_ISSUER=
JWT_AUDIENCE=




# ── Observability ──────────────────────────────────────────────────────────
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=archiet-ai-native-architecture-to-code-saas-platform
