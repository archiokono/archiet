
# GENERATED BY ARCHIE — entity: authenticationservice
# solution_id: 5373
# bundle_id: code_2026-04-06T18-28-31Z
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

from __future__ import annotations

import uuid
import logging
from http import HTTPStatus

from flask import Blueprint, jsonify, request
from flask_jwt_extended import jwt_required, get_jwt_identity
from marshmallow import ValidationError
from sqlalchemy import or_

from database import db
from app.models.authenticationservice import Authenticationservice
from app.schemas.authenticationservice_schema import (
    AuthenticationserviceSchema,
    AuthenticationserviceCreateSchema,
    AuthenticationserviceUpdateSchema,
)

logger = logging.getLogger(__name__)

authenticationservice_bp = Blueprint(
    "authenticationservice",
    __name__,
)

_schema = AuthenticationserviceSchema()
_schema_many = AuthenticationserviceSchema(many=True)
_create_schema = AuthenticationserviceCreateSchema()
_update_schema = AuthenticationserviceUpdateSchema()

# ---------------------------------------------------------------------------
# GET /  — list with pagination + optional search
# ---------------------------------------------------------------------------

@authenticationservice_bp.route("/", methods=["GET"])
@jwt_required()
def list_authenticationservices():
    """Return a paginated list of authenticationservice records."""
    page = request.args.get("page", 1, type=int)
    per_page = min(request.args.get("per_page", 20, type=int), 100)
    search = request.args.get("search", "", type=str).strip()

    query = Authenticationservice.query

    paginated = query.order_by(Authenticationservice.created_at.desc()).paginate(
        page=page, per_page=per_page, error_out=False
    )
    return jsonify(
        {
            "items": _schema_many.dump(paginated.items),
            "total": paginated.total,
            "page": paginated.page,
            "pages": paginated.pages,
            "per_page": paginated.per_page,
        }
    )

# ---------------------------------------------------------------------------
# GET /<id>  — single record
# ---------------------------------------------------------------------------

@authenticationservice_bp.route("/<string:item_id>", methods=["GET"])
@jwt_required()
def get_authenticationservice(item_id: str):
    item = db.session.get(Authenticationservice, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# POST /  — create
# ---------------------------------------------------------------------------

@authenticationservice_bp.route("/", methods=["POST"])
@jwt_required()
def create_authenticationservice():
    try:
        data = _create_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    item = Authenticationservice(
        id=str(uuid.uuid4()),

        **{k: v for k, v in data.items() if v is not None},
    )
    db.session.add(item)
    db.session.commit()
    logger.info("Created authenticationservice id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item)), HTTPStatus.CREATED

# ---------------------------------------------------------------------------
# PUT /<id>  — full update
# ---------------------------------------------------------------------------

@authenticationservice_bp.route("/<string:item_id>", methods=["PUT"])
@jwt_required()
def update_authenticationservice(item_id: str):
    item = db.session.get(Authenticationservice, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND

    try:
        data = _update_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    for key, value in data.items():
        if value is not None:
            setattr(item, key, value)

    db.session.commit()
    logger.info("Updated authenticationservice id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# DELETE /<id>
# ---------------------------------------------------------------------------

@authenticationservice_bp.route("/<string:item_id>", methods=["DELETE"])
@jwt_required()
def delete_authenticationservice(item_id: str):
    item = db.session.get(Authenticationservice, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    db.session.delete(item)
    db.session.commit()
    logger.info("Deleted authenticationservice id=%s user=%s", item_id, get_jwt_identity())
    return "", HTTPStatus.NO_CONTENT
