{# app/modules/solutions_product/templates/python_flask/database.py.j2 #}
# GENERATED BY ARCHIE
# solution_id: {{ solution_id }}
# bundle_id: {{ bundle_id }}
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate

db = SQLAlchemy()
migrate = Migrate()


def init_db(app):
    """Bind db + migrate to the Flask app instance."""
    db.init_app(app)
    migrate.init_app(app, db)

    # Import all models so their tables register on db.metadata
    import app.models.entities  # noqa: F401
    try:
        import app.models.user  # noqa: F401
    except ImportError:
        pass

    with app.app_context():
        db.create_all()
