{# java_spring_boot/TenantContext.java.j2 #}
// GENERATED BY ARCHIE — Architectural Genome v1.0.0
package com.archie.generated.{{ java_package }}.config;

/**
 * Thread-local holder for the current tenant (organization) ID.
 * Set by TenantFilter, read by repositories and audit logging.
 */
public final class TenantContext {

    private static final ThreadLocal<String> CURRENT_TENANT = new ThreadLocal<>();

    private TenantContext() {}

    public static String getCurrentTenant() {
        return CURRENT_TENANT.get();
    }

    public static void setCurrentTenant(String tenantId) {
        CURRENT_TENANT.set(tenantId);
    }

    public static void clear() {
        CURRENT_TENANT.remove();
    }
}
