# API base URL (no trailing slash)
# Leave EMPTY when running via Docker (Next.js proxy handles /api/* and /auth/*).
# Set to http://localhost:8000 only when running the backend directly without Docker.
NEXT_PUBLIC_API_URL=
