# App Store Connect Metadata — {{ solution_name }}

Generated by A.R.C.H.I.E. Fill in the bracketed fields before submission.

---

## App Information

| Field | Value | Limit |
|---|---|---|
| **Name** | {{ solution_name | truncate(30, true, '') }} | 30 chars |
| **Subtitle** | [One-line value proposition] | 30 chars |
| **Bundle ID** | {{ bundle_id }} | — |
| **SKU** | {{ solution_slug }}-ios-v1 | — |
| **Primary Language** | English (UK / US) | — |
| **Category** | Business | — |
| **Age Rating** | 4+ | — |

---

## App Description (4000 chars max)

{{ solution_name }} is an enterprise-grade application that manages:

{% for cls in classes %}
- **{{ cls.name | replace('_', ' ') | title }}** — {{ cls.description | default('Track and manage ' + (cls.name | replace('_', ' ') | lower) + ' records.') }}
{% endfor %}

Features:
- Real-time data sync with your {{ solution_name }} API
- Offline support — read and queue changes when disconnected
{% if has_biometric %}- Face ID / Touch ID authentication{% endif %}
{% if push_enabled %}- Push notifications for important updates{% endif %}
- Role-based access control
- Full CRUD operations with validation

[Add more detail about your specific use case here.]

---

## Keywords (100 chars max, comma-separated)

enterprise, {{ classes | map(attribute='name') | map('lower') | map('replace', '_', ' ') | join(', ') | truncate(80, true, '') }}

---

## Support URL

{{ terms_url | replace('terms', 'support') }}

---

## Marketing URL

[https://your-domain.com]

---

## Privacy Policy URL (required)

{{ privacy_policy_url }}

---

## Version Release Notes (What's New)

Version 1.0 — Initial release.

---

## Review Information

**Sign-in required:** {% if auth == "jwt-local" %}Yes{% else %}No{% endif %}
{% if auth == "jwt-local" %}
**Demo credentials:** [Provide a test account that doesn't contain real data]
- Email: reviewer@demo.{{ solution_slug }}.com
- Password: [Set a demo password]
{% endif %}

**Notes for reviewer:** This is an enterprise line-of-business application. All features require a connection to the {{ solution_name }} API server at {{ api_base_url }}.

---

## Screenshots Required

See `SCREENSHOTS.md` for exact specifications.
