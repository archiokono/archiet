// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}
// DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

package com.archie.generated.{{ java_package }}.repository;

{% for model_name, model_def in models.items() %}
import com.archie.generated.{{ java_package }}.model.{{ model_name }};
{% endfor %}
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

import java.time.Instant;
import java.util.List;
import java.util.Optional;

{% for model_name, model_def in models.items() %}
/**
 * Spring Data JPA repository for {{ model_name }}.
 */
@Repository
public interface {{ model_name }}Repository extends JpaRepository<{{ model_name }}, Long>, JpaSpecificationExecutor<{{ model_name }}> {
{% for field in model_def.fields -%}
{%- if field.name != 'id' -%}
{%- set ucFirst = field.name[0] | upper ~ field.name[1:] -%}
{%- if field.type in ('string', 'str', 'text') or field.type not in ['integer', 'int', 'number', 'decimal', 'float', 'Decimal', 'boolean', 'bool', 'timestamp', 'datetime', 'date', 'json', 'enum', 'uuid', 'UUID'] %}
{%- if field.unique %}

    Optional<{{ model_name }}> findBy{{ ucFirst }}(String {{ field.name }});
{%- else %}

    List<{{ model_name }}> findBy{{ ucFirst }}ContainingIgnoreCase(String {{ field.name }});
{%- endif %}
{%- elif field.type in ('boolean', 'bool') %}

    List<{{ model_name }}> findBy{{ ucFirst }}(Boolean {{ field.name }});
{%- endif %}
{%- endif %}
{%- endfor %}

    List<{{ model_name }}> findByCreatedAtBetween(Instant start, Instant end);
}

{% endfor %}
