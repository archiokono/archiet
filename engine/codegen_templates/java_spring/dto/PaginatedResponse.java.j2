{# java_spring/dto/PaginatedResponse.java.j2 #}
{# Generic PaginatedResponse<T> with items, total, page, perPage, pages. #}
{# Context: solution_id, bundle_id, java_package #}
// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}
// DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

package com.archie.generated.{{ java_package }}.dto;

import org.springframework.data.domain.Page;

import java.util.List;
import java.util.function.Function;

/**
 * Generic paginated response wrapper compatible with Spring Data Page.
 */
public record PaginatedResponse<T>(
    List<T> items,
    long total,
    int page,
    int perPage,
    int pages,
    boolean hasNext
) {

    /**
     * Build a PaginatedResponse from a Spring Data Page, mapping elements with the given mapper.
     */
    public static <E, T> PaginatedResponse<T> of(Page<E> springPage, Function<E, T> mapper) {
        List<T> items = springPage.getContent().stream().map(mapper).toList();
        return new PaginatedResponse<>(
            items,
            springPage.getTotalElements(),
            springPage.getNumber(),
            springPage.getSize(),
            springPage.getTotalPages(),
            springPage.hasNext()
        );
    }

    /**
     * Build a PaginatedResponse from a Spring Data Page (no mapping).
     */
    public static <T> PaginatedResponse<T> of(Page<T> springPage) {
        return of(springPage, Function.identity());
    }
}
