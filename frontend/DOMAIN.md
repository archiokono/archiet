# Domain Model & Vocabulary — Archiet — AI-Native Architecture-to-Code SaaS Platform

> **LLM AGENT INSTRUCTION**: Use this file to understand the problem domain BEFORE naming
> anything or making modelling decisions. Renaming a domain concept (e.g., "Customer" → "User")
> is a domain error, not a refactor. Every term in this glossary has a specific meaning
> agreed with the business stakeholder.

Generated by A.R.C.H.I.E. · Spec hash: `81750cb319c0fa45`

---

## Problem Statement

## Core Entities
Workspace, User, Blueprint, JourneySession, GenomeSpec, ArchitectureTemplate, CodeGeneration, Export, WorkspaceUsage, ApiKey, BillingSubscription, GitHubConnection, Referral, BlueprintVersion, BlueprintComment, ChangelogEntry

## Access Model
Owner: manages billing, invites team, owns all blueprints
Admin: manages members, creates and approves blueprints
Member: creates blueprints and triggers code generation
End-User (public): views shared public blueprint pages
System (API key holder): calls REST API for programmatic blueprint creation

## Business Rules
GDPR: user data must be fully deleted within 30 days of account closure (DELETE /account/delete cascade)
Usage metering: hard limits enforced on every AI call — free tier max 3 blueprints and 100 AI calls/month
ToS: users cannot access any route until tos_accepted_at is set
API rate limiting: 100 req/min free, 1000 req/min paid
All workspace data is row-level isolated — no cross-workspace queries allowed

## Problem Description
Archiet is an AI-native SaaS product that transforms plain English problem descriptions into production-ready, downloadable codebases. Target users are startup founders, freelance architects, and SME technical leads who need to go from idea to working software without an enterprise EA team.

The platform operates as a multi-tenant SaaS with workspace-level isolation. Each Workspace belongs to one billing tier (free, starter, professional, team) with enforced limits on blueprint count, AI call budget, and feature flag access. Workspaces have a trial_ends_at date and transition through states: trial → active → suspended → cancelled.

Users belong to a Workspace with roles: owner, admin, member. Users authenticate via email/password or GitHub OAuth. User records carry tos_accepted_at, tos_version, ui_mode (starter/expert), and gdpr_data_export_requested_at. Authentication uses JWT (access token 15min, refresh token 30 days, stored in httpOnly cookies). Users cannot access the 


---

## Domain Glossary

> Terms are inferred from entity names. Update the ArchiMate Meaning elements for authoritative definitions.


| Term | Definition |
|------|-----------|

| **SmtpRelayGcp** | Core domain entity — see `app/models/smtp_relay_gcp.py` and `app/schemas/smtp_relay_gcp_schema.py` |

| **PrimaryDataStore** | Core domain entity — see `app/models/primary_data_store.py` and `app/schemas/primary_data_store_schema.py` |

| **IdentityAndAccessManagement** | Core domain entity — see `app/models/identity_and_access_management.py` and `app/schemas/identity_and_access_management_schema.py` |

| **AuthenticationService** | Core domain entity — see `app/models/authentication_service.py` and `app/schemas/authentication_service_schema.py` |

| **MonitoringPlatform** | Core domain entity — see `app/models/monitoring_platform.py` and `app/schemas/monitoring_platform_schema.py` |

| **AnalyticsEngine** | Core domain entity — see `app/models/analytics_engine.py` and `app/schemas/analytics_engine_schema.py` |

| **ReportingService** | Core domain entity — see `app/models/reporting_service.py` and `app/schemas/reporting_service_schema.py` |

| **GcpCohesityForSystemsBackup** | Core domain entity — see `app/models/gcp_cohesity_for_systems_backup.py` and `app/schemas/gcp_cohesity_for_systems_backup_schema.py` |

| **ApiGateway** | Core domain entity — see `app/models/api_gateway.py` and `app/schemas/api_gateway_schema.py` |

| **MessageBroker** | Core domain entity — see `app/models/message_broker.py` and `app/schemas/message_broker_schema.py` |

| **BusinessLogicLayer** | Core domain entity — see `app/models/business_logic_layer.py` and `app/schemas/business_logic_layer_schema.py` |

| **CoreBusinessFunction** | Core domain entity — see `app/models/core_business_function.py` and `app/schemas/core_business_function_schema.py` |

| **CoreBusinessApi** | Core domain entity — see `app/models/core_business_api.py` and `app/schemas/core_business_api_schema.py` |

| **FrontendApplication** | Core domain entity — see `app/models/frontend_application.py` and `app/schemas/frontend_application_schema.py` |

| **ActiveDirectoryAd** | Core domain entity — see `app/models/active_directory_ad.py` and `app/schemas/active_directory_ad_schema.py` |

| **NotificationService** | Core domain entity — see `app/models/notification_service.py` and `app/schemas/notification_service_schema.py` |

| **GoogleGeminiModels** | Core domain entity — see `app/models/google_gemini_models.py` and `app/schemas/google_gemini_models_schema.py` |

| **VeritasNetbackupSaaS** | Core domain entity — see `app/models/veritas_netbackup_saa_s.py` and `app/schemas/veritas_netbackup_saa_s_schema.py` |

| **BlueprintJourneyInterface** | Core domain entity — see `app/models/blueprint_journey_interface.py` and `app/schemas/blueprint_journey_interface_schema.py` |

| **ArchitectureTemplateGallery** | Core domain entity — see `app/models/architecture_template_gallery.py` and `app/schemas/architecture_template_gallery_schema.py` |

| **BlueprintPublicViewer** | Core domain entity — see `app/models/blueprint_public_viewer.py` and `app/schemas/blueprint_public_viewer_schema.py` |

| **CicdToolchain** | Core domain entity — see `app/models/cicd_toolchain.py` and `app/schemas/cicd_toolchain_schema.py` |

| **CicdPipeline** | Core domain entity — see `app/models/cicd_pipeline.py` and `app/schemas/cicd_pipeline_schema.py` |

| **HealthMonitoring** | Core domain entity — see `app/models/health_monitoring.py` and `app/schemas/health_monitoring_schema.py` |

| **GenomeSpecGenerator** | Core domain entity — see `app/models/genome_spec_generator.py` and `app/schemas/genome_spec_generator_schema.py` |

| **CodeGenerationService** | Core domain entity — see `app/models/code_generation_service.py` and `app/schemas/code_generation_service_schema.py` |

| **ExportService** | Core domain entity — see `app/models/export_service.py` and `app/schemas/export_service_schema.py` |

| **JourneySessionOrchestrator** | Core domain entity — see `app/models/journey_session_orchestrator.py` and `app/schemas/journey_session_orchestrator_schema.py` |

| **StripeWebhookHandler** | Core domain entity — see `app/models/stripe_webhook_handler.py` and `app/schemas/stripe_webhook_handler_schema.py` |

| **RowLevelSecurityEnforcer** | Core domain entity — see `app/models/row_level_security_enforcer.py` and `app/schemas/row_level_security_enforcer_schema.py` |

| **GdprComplianceService** | Core domain entity — see `app/models/gdpr_compliance_service.py` and `app/schemas/gdpr_compliance_service_schema.py` |

| **AiArchitectureEngine** | Core domain entity — see `app/models/ai_architecture_engine.py` and `app/schemas/ai_architecture_engine_schema.py` |

| **UsageMeteringLimiting** | Core domain entity — see `app/models/usage_metering_limiting.py` and `app/schemas/usage_metering_limiting_schema.py` |

| **QualityScoringEngine** | Core domain entity — see `app/models/quality_scoring_engine.py` and `app/schemas/quality_scoring_engine_schema.py` |

| **GitHubIntegrationService** | Core domain entity — see `app/models/git_hub_integration_service.py` and `app/schemas/git_hub_integration_service_schema.py` |

| **CoreBusiness** | Core domain entity — see `app/models/core_business.py` and `app/schemas/core_business_schema.py` |

| **KeyMetricsAndKpIs** | Core domain entity — see `app/models/key_metrics_and_kp_is.py` and `app/schemas/key_metrics_and_kp_is_schema.py` |

| **AnalyticsDataStore** | Core domain entity — see `app/models/analytics_data_store.py` and `app/schemas/analytics_data_store_schema.py` |

| **Workspace** | Core domain entity — see `app/models/workspace.py` and `app/schemas/workspace_schema.py` |

| **JourneySessionConversations** | Core domain entity — see `app/models/journey_session_conversations.py` and `app/schemas/journey_session_conversations_schema.py` |

| **GenomeSpecDefinitions** | Core domain entity — see `app/models/genome_spec_definitions.py` and `app/schemas/genome_spec_definitions_schema.py` |

| **GeneratedCodeArtifacts** | Core domain entity — see `app/models/generated_code_artifacts.py` and `app/schemas/generated_code_artifacts_schema.py` |




---

## Business Capabilities & Their Implementation


> Capabilities inferred from Application Services in the bundle.

| Capability | Implemented By | API Prefix |
|------------|----------------|------------|

| **SmtpRelayGcp** | `app/routes/smtprelaygcp_routes.py` | `/api/v1/smtprelaygcps` |

| **PrimaryDataStore** | `app/routes/primarydatastore_routes.py` | `/api/v1/primarydatastores` |

| **IdentityAndAccessManagement** | `app/routes/identityandaccessmanagement_routes.py` | `/api/v1/identityandaccessmanagements` |

| **AuthenticationService** | `app/routes/authenticationservice_routes.py` | `/api/v1/authenticationservices` |

| **MonitoringPlatform** | `app/routes/monitoringplatform_routes.py` | `/api/v1/monitoringplatforms` |

| **AnalyticsEngine** | `app/routes/analyticsengine_routes.py` | `/api/v1/analyticsengines` |

| **ReportingService** | `app/routes/reportingservice_routes.py` | `/api/v1/reportingservices` |

| **GcpCohesityForSystemsBackup** | `app/routes/gcpcohesityforsystemsbackup_routes.py` | `/api/v1/gcpcohesityforsystemsbackups` |

| **ApiGateway** | `app/routes/apigateway_routes.py` | `/api/v1/apigateways` |

| **MessageBroker** | `app/routes/messagebroker_routes.py` | `/api/v1/messagebrokers` |

| **BusinessLogicLayer** | `app/routes/businesslogiclayer_routes.py` | `/api/v1/businesslogiclayers` |

| **CoreBusinessFunction** | `app/routes/corebusinessfunction_routes.py` | `/api/v1/corebusinessfunctions` |

| **CoreBusinessApi** | `app/routes/corebusinessapi_routes.py` | `/api/v1/corebusinessapis` |

| **FrontendApplication** | `app/routes/frontendapplication_routes.py` | `/api/v1/frontendapplications` |

| **ActiveDirectoryAd** | `app/routes/activedirectoryad_routes.py` | `/api/v1/activedirectoryads` |

| **NotificationService** | `app/routes/notificationservice_routes.py` | `/api/v1/notificationservices` |

| **GoogleGeminiModels** | `app/routes/googlegeminimodels_routes.py` | `/api/v1/googlegeminimodelss` |

| **VeritasNetbackupSaaS** | `app/routes/veritasnetbackupsaas_routes.py` | `/api/v1/veritasnetbackupsaass` |

| **BlueprintJourneyInterface** | `app/routes/blueprintjourneyinterface_routes.py` | `/api/v1/blueprintjourneyinterfaces` |

| **ArchitectureTemplateGallery** | `app/routes/architecturetemplategallery_routes.py` | `/api/v1/architecturetemplategallerys` |

| **BlueprintPublicViewer** | `app/routes/blueprintpublicviewer_routes.py` | `/api/v1/blueprintpublicviewers` |

| **CicdToolchain** | `app/routes/cicdtoolchain_routes.py` | `/api/v1/cicdtoolchains` |

| **CicdPipeline** | `app/routes/cicdpipeline_routes.py` | `/api/v1/cicdpipelines` |

| **HealthMonitoring** | `app/routes/healthmonitoring_routes.py` | `/api/v1/healthmonitorings` |

| **GenomeSpecGenerator** | `app/routes/genomespecgenerator_routes.py` | `/api/v1/genomespecgenerators` |

| **CodeGenerationService** | `app/routes/codegenerationservice_routes.py` | `/api/v1/codegenerationservices` |

| **ExportService** | `app/routes/exportservice_routes.py` | `/api/v1/exportservices` |

| **JourneySessionOrchestrator** | `app/routes/journeysessionorchestrator_routes.py` | `/api/v1/journeysessionorchestrators` |

| **StripeWebhookHandler** | `app/routes/stripewebhookhandler_routes.py` | `/api/v1/stripewebhookhandlers` |

| **RowLevelSecurityEnforcer** | `app/routes/rowlevelsecurityenforcer_routes.py` | `/api/v1/rowlevelsecurityenforcers` |

| **GdprComplianceService** | `app/routes/gdprcomplianceservice_routes.py` | `/api/v1/gdprcomplianceservices` |

| **AiArchitectureEngine** | `app/routes/aiarchitectureengine_routes.py` | `/api/v1/aiarchitectureengines` |

| **UsageMeteringLimiting** | `app/routes/usagemeteringlimiting_routes.py` | `/api/v1/usagemeteringlimitings` |

| **QualityScoringEngine** | `app/routes/qualityscoringengine_routes.py` | `/api/v1/qualityscoringengines` |

| **GitHubIntegrationService** | `app/routes/githubintegrationservice_routes.py` | `/api/v1/githubintegrationservices` |

| **CoreBusiness** | `app/routes/corebusiness_routes.py` | `/api/v1/corebusinesss` |

| **KeyMetricsAndKpIs** | `app/routes/keymetricsandkpis_routes.py` | `/api/v1/keymetricsandkpiss` |

| **AnalyticsDataStore** | `app/routes/analyticsdatastore_routes.py` | `/api/v1/analyticsdatastores` |

| **Workspace** | `app/routes/workspace_routes.py` | `/api/v1/workspaces` |

| **JourneySessionConversations** | `app/routes/journeysessionconversations_routes.py` | `/api/v1/journeysessionconversationss` |

| **GenomeSpecDefinitions** | `app/routes/genomespecdefinitions_routes.py` | `/api/v1/genomespecdefinitionss` |

| **GeneratedCodeArtifacts** | `app/routes/generatedcodeartifacts_routes.py` | `/api/v1/generatedcodeartifactss` |




---

## Entity Relationships

> How domain entities relate to each other. Use this to understand data integrity rules
> and cascade behavior before changing foreign keys or deleting records.


```





















































































```



| Entity | Table | Key Fields | Relates To |
|--------|-------|-----------|------------|

| `SmtpRelayGcp` | `smtp_relay_gcps` |  | — |

| `PrimaryDataStore` | `primary_data_stores` |  | — |

| `IdentityAndAccessManagement` | `identity_and_access_managements` |  | — |

| `AuthenticationService` | `authentication_services` |  | — |

| `MonitoringPlatform` | `monitoring_platforms` |  | — |

| `AnalyticsEngine` | `analytics_engines` |  | — |

| `ReportingService` | `reporting_services` |  | — |

| `GcpCohesityForSystemsBackup` | `gcp_cohesity_for_systems_backups` |  | — |

| `ApiGateway` | `api_gateways` |  | — |

| `MessageBroker` | `message_brokers` |  | — |

| `BusinessLogicLayer` | `business_logic_layers` |  | — |

| `CoreBusinessFunction` | `core_business_functions` |  | — |

| `CoreBusinessApi` | `core_business_apis` |  | — |

| `FrontendApplication` | `frontend_applications` |  | — |

| `ActiveDirectoryAd` | `active_directory_ads` |  | — |

| `NotificationService` | `notification_services` |  | — |

| `GoogleGeminiModels` | `google_gemini_models` |  | — |

| `VeritasNetbackupSaaS` | `veritas_netbackup_saa_s` |  | — |

| `BlueprintJourneyInterface` | `blueprint_journey_interfaces` |  | — |

| `ArchitectureTemplateGallery` | `architecture_template_galleries` |  | — |

| `BlueprintPublicViewer` | `blueprint_public_viewers` |  | — |

| `CicdToolchain` | `cicd_toolchains` |  | — |

| `CicdPipeline` | `cicd_pipelines` |  | — |

| `HealthMonitoring` | `health_monitorings` |  | — |

| `GenomeSpecGenerator` | `genome_spec_generators` |  | — |

| `CodeGenerationService` | `code_generation_services` |  | — |

| `ExportService` | `export_services` |  | — |

| `JourneySessionOrchestrator` | `journey_session_orchestrators` |  | — |

| `StripeWebhookHandler` | `stripe_webhook_handlers` |  | — |

| `RowLevelSecurityEnforcer` | `row_level_security_enforcers` |  | — |

| `GdprComplianceService` | `gdpr_compliance_services` |  | — |

| `AiArchitectureEngine` | `ai_architecture_engines` |  | — |

| `UsageMeteringLimiting` | `usage_metering_limitings` |  | — |

| `QualityScoringEngine` | `quality_scoring_engines` |  | — |

| `GitHubIntegrationService` | `git_hub_integration_services` |  | — |

| `CoreBusiness` | `core_businesses` |  | — |

| `KeyMetricsAndKpIs` | `key_metrics_and_kp_es` |  | — |

| `AnalyticsDataStore` | `analytics_data_stores` |  | — |

| `Workspace` | `workspaces` |  | — |

| `JourneySessionConversations` | `journey_session_conversations` |  | — |

| `GenomeSpecDefinitions` | `genome_spec_definitions` |  | — |

| `GeneratedCodeArtifacts` | `generated_code_artifacts` |  | — |



---

## Value Streams


> Value streams inferred from business process flows.



**SmtpRelayGcp lifecycle**:
```

1. GET /api/smtp_relay_gcps — List SmtpRelayGcps

2. POST /api/smtp_relay_gcps — Create SmtpRelayGcp

3. GET /api/smtp_relay_gcps/{id} — Get SmtpRelayGcp by ID

4. PUT /api/smtp_relay_gcps/{id} — Update SmtpRelayGcp

5. DELETE /api/smtp_relay_gcps/{id} — Delete SmtpRelayGcp

```



**PrimaryDataStore lifecycle**:
```

1. GET /api/primary_data_stores — List PrimaryDataStores

2. POST /api/primary_data_stores — Create PrimaryDataStore

3. GET /api/primary_data_stores/{id} — Get PrimaryDataStore by ID

4. PUT /api/primary_data_stores/{id} — Update PrimaryDataStore

5. DELETE /api/primary_data_stores/{id} — Delete PrimaryDataStore

```



**IdentityAndAccessManagement lifecycle**:
```

1. GET /api/identity_and_access_managements — List IdentityAndAccessManagements

2. POST /api/identity_and_access_managements — Create IdentityAndAccessManagement

3. GET /api/identity_and_access_managements/{id} — Get IdentityAndAccessManagement by ID

4. PUT /api/identity_and_access_managements/{id} — Update IdentityAndAccessManagement

5. DELETE /api/identity_and_access_managements/{id} — Delete IdentityAndAccessManagement

```



**AuthenticationService lifecycle**:
```

1. GET /api/authentication_services — List AuthenticationServices

2. POST /api/authentication_services — Create AuthenticationService

3. GET /api/authentication_services/{id} — Get AuthenticationService by ID

4. PUT /api/authentication_services/{id} — Update AuthenticationService

5. DELETE /api/authentication_services/{id} — Delete AuthenticationService

```



**MonitoringPlatform lifecycle**:
```

1. GET /api/monitoring_platforms — List MonitoringPlatforms

2. POST /api/monitoring_platforms — Create MonitoringPlatform

3. GET /api/monitoring_platforms/{id} — Get MonitoringPlatform by ID

4. PUT /api/monitoring_platforms/{id} — Update MonitoringPlatform

5. DELETE /api/monitoring_platforms/{id} — Delete MonitoringPlatform

```



**AnalyticsEngine lifecycle**:
```

1. GET /api/analytics_engines — List AnalyticsEngines

2. POST /api/analytics_engines — Create AnalyticsEngine

3. GET /api/analytics_engines/{id} — Get AnalyticsEngine by ID

4. PUT /api/analytics_engines/{id} — Update AnalyticsEngine

5. DELETE /api/analytics_engines/{id} — Delete AnalyticsEngine

```



**ReportingService lifecycle**:
```

1. GET /api/reporting_services — List ReportingServices

2. POST /api/reporting_services — Create ReportingService

3. GET /api/reporting_services/{id} — Get ReportingService by ID

4. PUT /api/reporting_services/{id} — Update ReportingService

5. DELETE /api/reporting_services/{id} — Delete ReportingService

```



**GcpCohesityForSystemsBackup lifecycle**:
```

1. GET /api/gcp_cohesity_for_systems_backups — List GcpCohesityForSystemsBackups

2. POST /api/gcp_cohesity_for_systems_backups — Create GcpCohesityForSystemsBackup

3. GET /api/gcp_cohesity_for_systems_backups/{id} — Get GcpCohesityForSystemsBackup by ID

4. PUT /api/gcp_cohesity_for_systems_backups/{id} — Update GcpCohesityForSystemsBackup

5. DELETE /api/gcp_cohesity_for_systems_backups/{id} — Delete GcpCohesityForSystemsBackup

```



**ApiGateway lifecycle**:
```

1. GET /api/api_gateways — List ApiGateways

2. POST /api/api_gateways — Create ApiGateway

3. GET /api/api_gateways/{id} — Get ApiGateway by ID

4. PUT /api/api_gateways/{id} — Update ApiGateway

5. DELETE /api/api_gateways/{id} — Delete ApiGateway

```



**MessageBroker lifecycle**:
```

1. GET /api/message_brokers — List MessageBrokers

2. POST /api/message_brokers — Create MessageBroker

3. GET /api/message_brokers/{id} — Get MessageBroker by ID

4. PUT /api/message_brokers/{id} — Update MessageBroker

5. DELETE /api/message_brokers/{id} — Delete MessageBroker

```



**BusinessLogicLayer lifecycle**:
```

1. GET /api/business_logic_layers — List BusinessLogicLayers

2. POST /api/business_logic_layers — Create BusinessLogicLayer

3. GET /api/business_logic_layers/{id} — Get BusinessLogicLayer by ID

4. PUT /api/business_logic_layers/{id} — Update BusinessLogicLayer

5. DELETE /api/business_logic_layers/{id} — Delete BusinessLogicLayer

```



**CoreBusinessFunction lifecycle**:
```

1. GET /api/core_business_functions — List CoreBusinessFunctions

2. POST /api/core_business_functions — Create CoreBusinessFunction

3. GET /api/core_business_functions/{id} — Get CoreBusinessFunction by ID

4. PUT /api/core_business_functions/{id} — Update CoreBusinessFunction

5. DELETE /api/core_business_functions/{id} — Delete CoreBusinessFunction

```



**CoreBusinessApi lifecycle**:
```

1. GET /api/core_business_apis — List CoreBusinessApis

2. POST /api/core_business_apis — Create CoreBusinessApi

3. GET /api/core_business_apis/{id} — Get CoreBusinessApi by ID

4. PUT /api/core_business_apis/{id} — Update CoreBusinessApi

5. DELETE /api/core_business_apis/{id} — Delete CoreBusinessApi

```



**FrontendApplication lifecycle**:
```

1. GET /api/frontend_applications — List FrontendApplications

2. POST /api/frontend_applications — Create FrontendApplication

3. GET /api/frontend_applications/{id} — Get FrontendApplication by ID

4. PUT /api/frontend_applications/{id} — Update FrontendApplication

5. DELETE /api/frontend_applications/{id} — Delete FrontendApplication

```



**ActiveDirectoryAd lifecycle**:
```

1. GET /api/active_directory_ads — List ActiveDirectoryAds

2. POST /api/active_directory_ads — Create ActiveDirectoryAd

3. GET /api/active_directory_ads/{id} — Get ActiveDirectoryAd by ID

4. PUT /api/active_directory_ads/{id} — Update ActiveDirectoryAd

5. DELETE /api/active_directory_ads/{id} — Delete ActiveDirectoryAd

```



**NotificationService lifecycle**:
```

1. GET /api/notification_services — List NotificationServices

2. POST /api/notification_services — Create NotificationService

3. GET /api/notification_services/{id} — Get NotificationService by ID

4. PUT /api/notification_services/{id} — Update NotificationService

5. DELETE /api/notification_services/{id} — Delete NotificationService

```



**GoogleGeminiModels lifecycle**:
```

1. GET /api/google_gemini_models — List GoogleGeminiModelss

2. POST /api/google_gemini_models — Create GoogleGeminiModels

3. GET /api/google_gemini_models/{id} — Get GoogleGeminiModels by ID

4. PUT /api/google_gemini_models/{id} — Update GoogleGeminiModels

5. DELETE /api/google_gemini_models/{id} — Delete GoogleGeminiModels

```



**VeritasNetbackupSaaS lifecycle**:
```

1. GET /api/veritas_netbackup_saa_s — List VeritasNetbackupSaaSs

2. POST /api/veritas_netbackup_saa_s — Create VeritasNetbackupSaaS

3. GET /api/veritas_netbackup_saa_s/{id} — Get VeritasNetbackupSaaS by ID

4. PUT /api/veritas_netbackup_saa_s/{id} — Update VeritasNetbackupSaaS

5. DELETE /api/veritas_netbackup_saa_s/{id} — Delete VeritasNetbackupSaaS

```



**BlueprintJourneyInterface lifecycle**:
```

1. GET /api/blueprint_journey_interfaces — List BlueprintJourneyInterfaces

2. POST /api/blueprint_journey_interfaces — Create BlueprintJourneyInterface

3. GET /api/blueprint_journey_interfaces/{id} — Get BlueprintJourneyInterface by ID

4. PUT /api/blueprint_journey_interfaces/{id} — Update BlueprintJourneyInterface

5. DELETE /api/blueprint_journey_interfaces/{id} — Delete BlueprintJourneyInterface

```



**ArchitectureTemplateGallery lifecycle**:
```

1. GET /api/architecture_template_galleries — List ArchitectureTemplateGallerys

2. POST /api/architecture_template_galleries — Create ArchitectureTemplateGallery

3. GET /api/architecture_template_galleries/{id} — Get ArchitectureTemplateGallery by ID

4. PUT /api/architecture_template_galleries/{id} — Update ArchitectureTemplateGallery

5. DELETE /api/architecture_template_galleries/{id} — Delete ArchitectureTemplateGallery

```



**BlueprintPublicViewer lifecycle**:
```

1. GET /api/blueprint_public_viewers — List BlueprintPublicViewers

2. POST /api/blueprint_public_viewers — Create BlueprintPublicViewer

3. GET /api/blueprint_public_viewers/{id} — Get BlueprintPublicViewer by ID

4. PUT /api/blueprint_public_viewers/{id} — Update BlueprintPublicViewer

5. DELETE /api/blueprint_public_viewers/{id} — Delete BlueprintPublicViewer

```



**CicdToolchain lifecycle**:
```

1. GET /api/cicd_toolchains — List CicdToolchains

2. POST /api/cicd_toolchains — Create CicdToolchain

3. GET /api/cicd_toolchains/{id} — Get CicdToolchain by ID

4. PUT /api/cicd_toolchains/{id} — Update CicdToolchain

5. DELETE /api/cicd_toolchains/{id} — Delete CicdToolchain

```



**CicdPipeline lifecycle**:
```

1. GET /api/cicd_pipelines — List CicdPipelines

2. POST /api/cicd_pipelines — Create CicdPipeline

3. GET /api/cicd_pipelines/{id} — Get CicdPipeline by ID

4. PUT /api/cicd_pipelines/{id} — Update CicdPipeline

5. DELETE /api/cicd_pipelines/{id} — Delete CicdPipeline

```



**HealthMonitoring lifecycle**:
```

1. GET /api/health_monitorings — List HealthMonitorings

2. POST /api/health_monitorings — Create HealthMonitoring

3. GET /api/health_monitorings/{id} — Get HealthMonitoring by ID

4. PUT /api/health_monitorings/{id} — Update HealthMonitoring

5. DELETE /api/health_monitorings/{id} — Delete HealthMonitoring

```



**GenomeSpecGenerator lifecycle**:
```

1. GET /api/genome_spec_generators — List GenomeSpecGenerators

2. POST /api/genome_spec_generators — Create GenomeSpecGenerator

3. GET /api/genome_spec_generators/{id} — Get GenomeSpecGenerator by ID

4. PUT /api/genome_spec_generators/{id} — Update GenomeSpecGenerator

5. DELETE /api/genome_spec_generators/{id} — Delete GenomeSpecGenerator

```



**CodeGenerationService lifecycle**:
```

1. GET /api/code_generation_services — List CodeGenerationServices

2. POST /api/code_generation_services — Create CodeGenerationService

3. GET /api/code_generation_services/{id} — Get CodeGenerationService by ID

4. PUT /api/code_generation_services/{id} — Update CodeGenerationService

5. DELETE /api/code_generation_services/{id} — Delete CodeGenerationService

```



**ExportService lifecycle**:
```

1. GET /api/export_services — List ExportServices

2. POST /api/export_services — Create ExportService

3. GET /api/export_services/{id} — Get ExportService by ID

4. PUT /api/export_services/{id} — Update ExportService

5. DELETE /api/export_services/{id} — Delete ExportService

```



**JourneySessionOrchestrator lifecycle**:
```

1. GET /api/journey_session_orchestrators — List JourneySessionOrchestrators

2. POST /api/journey_session_orchestrators — Create JourneySessionOrchestrator

3. GET /api/journey_session_orchestrators/{id} — Get JourneySessionOrchestrator by ID

4. PUT /api/journey_session_orchestrators/{id} — Update JourneySessionOrchestrator

5. DELETE /api/journey_session_orchestrators/{id} — Delete JourneySessionOrchestrator

```



**StripeWebhookHandler lifecycle**:
```

1. GET /api/stripe_webhook_handlers — List StripeWebhookHandlers

2. POST /api/stripe_webhook_handlers — Create StripeWebhookHandler

3. GET /api/stripe_webhook_handlers/{id} — Get StripeWebhookHandler by ID

4. PUT /api/stripe_webhook_handlers/{id} — Update StripeWebhookHandler

5. DELETE /api/stripe_webhook_handlers/{id} — Delete StripeWebhookHandler

```



**RowLevelSecurityEnforcer lifecycle**:
```

1. GET /api/row_level_security_enforcers — List RowLevelSecurityEnforcers

2. POST /api/row_level_security_enforcers — Create RowLevelSecurityEnforcer

3. GET /api/row_level_security_enforcers/{id} — Get RowLevelSecurityEnforcer by ID

4. PUT /api/row_level_security_enforcers/{id} — Update RowLevelSecurityEnforcer

5. DELETE /api/row_level_security_enforcers/{id} — Delete RowLevelSecurityEnforcer

```



**GdprComplianceService lifecycle**:
```

1. GET /api/gdpr_compliance_services — List GdprComplianceServices

2. POST /api/gdpr_compliance_services — Create GdprComplianceService

3. GET /api/gdpr_compliance_services/{id} — Get GdprComplianceService by ID

4. PUT /api/gdpr_compliance_services/{id} — Update GdprComplianceService

5. DELETE /api/gdpr_compliance_services/{id} — Delete GdprComplianceService

```



**AiArchitectureEngine lifecycle**:
```

1. GET /api/ai_architecture_engines — List AiArchitectureEngines

2. POST /api/ai_architecture_engines — Create AiArchitectureEngine

3. GET /api/ai_architecture_engines/{id} — Get AiArchitectureEngine by ID

4. PUT /api/ai_architecture_engines/{id} — Update AiArchitectureEngine

5. DELETE /api/ai_architecture_engines/{id} — Delete AiArchitectureEngine

```



**UsageMeteringLimiting lifecycle**:
```

1. GET /api/usage_metering_limitings — List UsageMeteringLimitings

2. POST /api/usage_metering_limitings — Create UsageMeteringLimiting

3. GET /api/usage_metering_limitings/{id} — Get UsageMeteringLimiting by ID

4. PUT /api/usage_metering_limitings/{id} — Update UsageMeteringLimiting

5. DELETE /api/usage_metering_limitings/{id} — Delete UsageMeteringLimiting

```



**QualityScoringEngine lifecycle**:
```

1. GET /api/quality_scoring_engines — List QualityScoringEngines

2. POST /api/quality_scoring_engines — Create QualityScoringEngine

3. GET /api/quality_scoring_engines/{id} — Get QualityScoringEngine by ID

4. PUT /api/quality_scoring_engines/{id} — Update QualityScoringEngine

5. DELETE /api/quality_scoring_engines/{id} — Delete QualityScoringEngine

```



**GitHubIntegrationService lifecycle**:
```

1. GET /api/git_hub_integration_services — List GitHubIntegrationServices

2. POST /api/git_hub_integration_services — Create GitHubIntegrationService

3. GET /api/git_hub_integration_services/{id} — Get GitHubIntegrationService by ID

4. PUT /api/git_hub_integration_services/{id} — Update GitHubIntegrationService

5. DELETE /api/git_hub_integration_services/{id} — Delete GitHubIntegrationService

```



**CoreBusiness lifecycle**:
```

1. GET /api/core_businesses — List CoreBusinessEntitys

2. POST /api/core_businesses — Create CoreBusinessEntity

3. GET /api/core_businesses/{id} — Get CoreBusinessEntity by ID

4. PUT /api/core_businesses/{id} — Update CoreBusinessEntity

5. DELETE /api/core_businesses/{id} — Delete CoreBusinessEntity

```



**KeyMetricsAndKpIs lifecycle**:
```

1. GET /api/key_metrics_and_kp_es — List KeyMetricsAndKpIss

2. POST /api/key_metrics_and_kp_es — Create KeyMetricsAndKpIs

3. GET /api/key_metrics_and_kp_es/{id} — Get KeyMetricsAndKpIs by ID

4. PUT /api/key_metrics_and_kp_es/{id} — Update KeyMetricsAndKpIs

5. DELETE /api/key_metrics_and_kp_es/{id} — Delete KeyMetricsAndKpIs

```



**AnalyticsDataStore lifecycle**:
```

1. GET /api/analytics_data_stores — List AnalyticsDataStores

2. POST /api/analytics_data_stores — Create AnalyticsDataStore

3. GET /api/analytics_data_stores/{id} — Get AnalyticsDataStore by ID

4. PUT /api/analytics_data_stores/{id} — Update AnalyticsDataStore

5. DELETE /api/analytics_data_stores/{id} — Delete AnalyticsDataStore

```



**Workspace lifecycle**:
```

1. GET /api/workspaces — List WorkspaceDatas

2. POST /api/workspaces — Create WorkspaceData

3. GET /api/workspaces/{id} — Get WorkspaceData by ID

4. PUT /api/workspaces/{id} — Update WorkspaceData

5. DELETE /api/workspaces/{id} — Delete WorkspaceData

```



**JourneySessionConversations lifecycle**:
```

1. GET /api/journey_session_conversations — List JourneySessionConversationss

2. POST /api/journey_session_conversations — Create JourneySessionConversations

3. GET /api/journey_session_conversations/{id} — Get JourneySessionConversations by ID

4. PUT /api/journey_session_conversations/{id} — Update JourneySessionConversations

5. DELETE /api/journey_session_conversations/{id} — Delete JourneySessionConversations

```



**GenomeSpecDefinitions lifecycle**:
```

1. GET /api/genome_spec_definitions — List GenomeSpecDefinitionss

2. POST /api/genome_spec_definitions — Create GenomeSpecDefinitions

3. GET /api/genome_spec_definitions/{id} — Get GenomeSpecDefinitions by ID

4. PUT /api/genome_spec_definitions/{id} — Update GenomeSpecDefinitions

5. DELETE /api/genome_spec_definitions/{id} — Delete GenomeSpecDefinitions

```



**GeneratedCodeArtifacts lifecycle**:
```

1. GET /api/generated_code_artifacts — List GeneratedCodeArtifactss

2. POST /api/generated_code_artifacts — Create GeneratedCodeArtifacts

3. GET /api/generated_code_artifacts/{id} — Get GeneratedCodeArtifacts by ID

4. PUT /api/generated_code_artifacts/{id} — Update GeneratedCodeArtifacts

5. DELETE /api/generated_code_artifacts/{id} — Delete GeneratedCodeArtifacts

```





---

## Stakeholders & Their Concerns


| Stakeholder | Type | Primary Concern | What They Must Not See Broken |
|-------------|------|----------------|-------------------------------|

| **smb technology Stakeholder** | Stakeholder |  | — |




---

## Business Events




---

## Anti-Patterns for This Domain

> Patterns that look reasonable but are WRONG for this specific domain.
> LLM agents: before applying a common pattern, check it's not listed here.

- **Merging `Customer` and `User`**: In this domain, a Customer is the organization paying for the subscription; a User is a human within that organization. They are separate entities with separate lifecycles.
- **Soft-deleting without audit**: Simply setting `deleted_at` loses the deletion reason and actor. Always record `deleted_by` and `deletion_reason`.
- **Storing derived data**: Totals, counts, and aggregates are computed at read time, not stored. Storing them creates consistency bugs.
- **Implicit tenant from auth context**: Always pass `organization_id` explicitly to service functions. Never infer it from a global or thread-local.


---

## Ubiquitous Language Quick Reference

> When writing code, use EXACTLY these names — not synonyms or abbreviations.



| Use this | Not this |
|----------|----------|

| `SmtpRelayGcp` | `SmtpRelayGcpRecord`, `SmtpRelayGcpData`, `SmtpRelayGcpModel`, `SmtpRelayGcpObj` |

| `PrimaryDataStore` | `PrimaryDataStoreRecord`, `PrimaryDataStoreData`, `PrimaryDataStoreModel`, `PrimaryDataStoreObj` |

| `IdentityAndAccessManagement` | `IdentityAndAccessManagementRecord`, `IdentityAndAccessManagementData`, `IdentityAndAccessManagementModel`, `IdentityAndAccessManagementObj` |

| `AuthenticationService` | `AuthenticationServiceRecord`, `AuthenticationServiceData`, `AuthenticationServiceModel`, `AuthenticationServiceObj` |

| `MonitoringPlatform` | `MonitoringPlatformRecord`, `MonitoringPlatformData`, `MonitoringPlatformModel`, `MonitoringPlatformObj` |

| `AnalyticsEngine` | `AnalyticsEngineRecord`, `AnalyticsEngineData`, `AnalyticsEngineModel`, `AnalyticsEngineObj` |

| `ReportingService` | `ReportingServiceRecord`, `ReportingServiceData`, `ReportingServiceModel`, `ReportingServiceObj` |

| `GcpCohesityForSystemsBackup` | `GcpCohesityForSystemsBackupRecord`, `GcpCohesityForSystemsBackupData`, `GcpCohesityForSystemsBackupModel`, `GcpCohesityForSystemsBackupObj` |

| `ApiGateway` | `ApiGatewayRecord`, `ApiGatewayData`, `ApiGatewayModel`, `ApiGatewayObj` |

| `MessageBroker` | `MessageBrokerRecord`, `MessageBrokerData`, `MessageBrokerModel`, `MessageBrokerObj` |

| `BusinessLogicLayer` | `BusinessLogicLayerRecord`, `BusinessLogicLayerData`, `BusinessLogicLayerModel`, `BusinessLogicLayerObj` |

| `CoreBusinessFunction` | `CoreBusinessFunctionRecord`, `CoreBusinessFunctionData`, `CoreBusinessFunctionModel`, `CoreBusinessFunctionObj` |

| `CoreBusinessApi` | `CoreBusinessApiRecord`, `CoreBusinessApiData`, `CoreBusinessApiModel`, `CoreBusinessApiObj` |

| `FrontendApplication` | `FrontendApplicationRecord`, `FrontendApplicationData`, `FrontendApplicationModel`, `FrontendApplicationObj` |

| `ActiveDirectoryAd` | `ActiveDirectoryAdRecord`, `ActiveDirectoryAdData`, `ActiveDirectoryAdModel`, `ActiveDirectoryAdObj` |

| `NotificationService` | `NotificationServiceRecord`, `NotificationServiceData`, `NotificationServiceModel`, `NotificationServiceObj` |

| `GoogleGeminiModels` | `GoogleGeminiModelsRecord`, `GoogleGeminiModelsData`, `GoogleGeminiModelsModel`, `GoogleGeminiModelsObj` |

| `VeritasNetbackupSaaS` | `VeritasNetbackupSaaSRecord`, `VeritasNetbackupSaaSData`, `VeritasNetbackupSaaSModel`, `VeritasNetbackupSaaSObj` |

| `BlueprintJourneyInterface` | `BlueprintJourneyInterfaceRecord`, `BlueprintJourneyInterfaceData`, `BlueprintJourneyInterfaceModel`, `BlueprintJourneyInterfaceObj` |

| `ArchitectureTemplateGallery` | `ArchitectureTemplateGalleryRecord`, `ArchitectureTemplateGalleryData`, `ArchitectureTemplateGalleryModel`, `ArchitectureTemplateGalleryObj` |

| `BlueprintPublicViewer` | `BlueprintPublicViewerRecord`, `BlueprintPublicViewerData`, `BlueprintPublicViewerModel`, `BlueprintPublicViewerObj` |

| `CicdToolchain` | `CicdToolchainRecord`, `CicdToolchainData`, `CicdToolchainModel`, `CicdToolchainObj` |

| `CicdPipeline` | `CicdPipelineRecord`, `CicdPipelineData`, `CicdPipelineModel`, `CicdPipelineObj` |

| `HealthMonitoring` | `HealthMonitoringRecord`, `HealthMonitoringData`, `HealthMonitoringModel`, `HealthMonitoringObj` |

| `GenomeSpecGenerator` | `GenomeSpecGeneratorRecord`, `GenomeSpecGeneratorData`, `GenomeSpecGeneratorModel`, `GenomeSpecGeneratorObj` |

| `CodeGenerationService` | `CodeGenerationServiceRecord`, `CodeGenerationServiceData`, `CodeGenerationServiceModel`, `CodeGenerationServiceObj` |

| `ExportService` | `ExportServiceRecord`, `ExportServiceData`, `ExportServiceModel`, `ExportServiceObj` |

| `JourneySessionOrchestrator` | `JourneySessionOrchestratorRecord`, `JourneySessionOrchestratorData`, `JourneySessionOrchestratorModel`, `JourneySessionOrchestratorObj` |

| `StripeWebhookHandler` | `StripeWebhookHandlerRecord`, `StripeWebhookHandlerData`, `StripeWebhookHandlerModel`, `StripeWebhookHandlerObj` |

| `RowLevelSecurityEnforcer` | `RowLevelSecurityEnforcerRecord`, `RowLevelSecurityEnforcerData`, `RowLevelSecurityEnforcerModel`, `RowLevelSecurityEnforcerObj` |

| `GdprComplianceService` | `GdprComplianceServiceRecord`, `GdprComplianceServiceData`, `GdprComplianceServiceModel`, `GdprComplianceServiceObj` |

| `AiArchitectureEngine` | `AiArchitectureEngineRecord`, `AiArchitectureEngineData`, `AiArchitectureEngineModel`, `AiArchitectureEngineObj` |

| `UsageMeteringLimiting` | `UsageMeteringLimitingRecord`, `UsageMeteringLimitingData`, `UsageMeteringLimitingModel`, `UsageMeteringLimitingObj` |

| `QualityScoringEngine` | `QualityScoringEngineRecord`, `QualityScoringEngineData`, `QualityScoringEngineModel`, `QualityScoringEngineObj` |

| `GitHubIntegrationService` | `GitHubIntegrationServiceRecord`, `GitHubIntegrationServiceData`, `GitHubIntegrationServiceModel`, `GitHubIntegrationServiceObj` |

| `CoreBusiness` | `CoreBusinessRecord`, `CoreBusinessData`, `CoreBusinessModel`, `CoreBusinessObj` |

| `KeyMetricsAndKpIs` | `KeyMetricsAndKpIsRecord`, `KeyMetricsAndKpIsData`, `KeyMetricsAndKpIsModel`, `KeyMetricsAndKpIsObj` |

| `AnalyticsDataStore` | `AnalyticsDataStoreRecord`, `AnalyticsDataStoreData`, `AnalyticsDataStoreModel`, `AnalyticsDataStoreObj` |

| `Workspace` | `WorkspaceRecord`, `WorkspaceData`, `WorkspaceModel`, `WorkspaceObj` |

| `JourneySessionConversations` | `JourneySessionConversationsRecord`, `JourneySessionConversationsData`, `JourneySessionConversationsModel`, `JourneySessionConversationsObj` |

| `GenomeSpecDefinitions` | `GenomeSpecDefinitionsRecord`, `GenomeSpecDefinitionsData`, `GenomeSpecDefinitionsModel`, `GenomeSpecDefinitionsObj` |

| `GeneratedCodeArtifacts` | `GeneratedCodeArtifactsRecord`, `GeneratedCodeArtifactsData`, `GeneratedCodeArtifactsModel`, `GeneratedCodeArtifactsObj` |

| `organization_id` | `tenant_id`, `company_id`, `account_id`, `org` |
| `created_at` | `date_created`, `timestamp`, `creation_date` |
| `is_active` | `active`, `enabled`, `status == "active"` |



---
*Auto-generated by A.R.C.H.I.E. from ArchiMate Strategy + Motivation + Application layers.*
*Solution: `5373` · Do not edit manually — regenerate from the platform.*
