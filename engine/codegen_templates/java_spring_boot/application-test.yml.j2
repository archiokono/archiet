# GENERATED BY ARCHIE — Test profile configuration
# solution_id: {{ solution_id }}
spring:
  datasource:
    url: jdbc:h2:mem:{{ solution_name | lower | replace(' ', '_') | replace('-', '_') }}_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  flyway:
    enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ""

# Symmetric JWT secret for tests (NOT for production)
security:
  jwt:
    secret: test-secret-key-minimum-256-bits-for-hmac-sha256-algorithm-requirement

logging:
  level:
    root: WARN
    com.archie.generated: DEBUG
