{# go_chi/dto/dto.go.j2 #}
{# CreateRequest, UpdateRequest, Response DTOs per entity with validate tags. #}
{# Context: solution_id, bundle_id, models (dict of model_name -> model_def) #}
// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}
// DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

package dto

import "time"

{% for model_name, model_def in models.items() %}
// {{ model_name }}CreateRequest is the payload for creating a {{ model_name }}.
type {{ model_name }}CreateRequest struct {
{% for field in model_def.fields %}
{% if field.name != 'id' and not field.readonly %}
{% if field.type in ('string', 'str', 'text', 'uuid', 'UUID') or field.format == 'uuid' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"{% if field.required %} validate:"required{% if field.max_length %},max={{ field.max_length }}{% endif %}"{% endif %}`
{% elif field.type in ('integer', 'int') %}
	{{ field.name | capitalize }} int64 `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type in ('number', 'decimal', 'float', 'Decimal') %}
	{{ field.name | capitalize }} float64 `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type in ('boolean', 'bool') %}
	{{ field.name | capitalize }} bool `json:"{{ field.name }}"`
{% elif field.type in ('timestamp', 'datetime', 'date') or field.format == 'date-time' %}
	{{ field.name | capitalize }} time.Time `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }}"`
{% else %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% endif %}
{% endif %}
{% endfor %}
}

// {{ model_name }}UpdateRequest is the payload for updating a {{ model_name }}.
type {{ model_name }}UpdateRequest struct {
{% for field in model_def.fields %}
{% if field.name != 'id' and not field.readonly %}
{% if field.type in ('string', 'str', 'text', 'uuid', 'UUID') or field.format == 'uuid' %}
	{{ field.name | capitalize }} *string `json:"{{ field.name }},omitempty"`
{% elif field.type in ('integer', 'int') %}
	{{ field.name | capitalize }} *int64 `json:"{{ field.name }},omitempty"`
{% elif field.type in ('number', 'decimal', 'float', 'Decimal') %}
	{{ field.name | capitalize }} *float64 `json:"{{ field.name }},omitempty"`
{% elif field.type in ('boolean', 'bool') %}
	{{ field.name | capitalize }} *bool `json:"{{ field.name }},omitempty"`
{% elif field.type in ('timestamp', 'datetime', 'date') or field.format == 'date-time' %}
	{{ field.name | capitalize }} *time.Time `json:"{{ field.name }},omitempty"`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} *string `json:"{{ field.name }},omitempty"`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }},omitempty"`
{% else %}
	{{ field.name | capitalize }} *string `json:"{{ field.name }},omitempty"`
{% endif %}
{% endif %}
{% endfor %}
}

// {{ model_name }}Response is the JSON response for a {{ model_name }}.
type {{ model_name }}Response struct {
	ID             string  `json:"id"`
{% for field in model_def.fields %}
{% if field.name != 'id' %}
{% if field.type in ('string', 'str', 'text', 'uuid', 'UUID') or field.format == 'uuid' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% elif field.type in ('integer', 'int') %}
	{{ field.name | capitalize }} int64 `json:"{{ field.name }}"`
{% elif field.type in ('number', 'decimal', 'float', 'Decimal') %}
	{{ field.name | capitalize }} float64 `json:"{{ field.name }}"`
{% elif field.type in ('boolean', 'bool') %}
	{{ field.name | capitalize }} bool `json:"{{ field.name }}"`
{% elif field.type in ('timestamp', 'datetime', 'date') or field.format == 'date-time' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }}"`
{% else %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% endif %}
{% endif %}
{% endfor %}
	OrganizationID string `json:"organization_id"`
	CreatedAt      string `json:"created_at"`
	UpdatedAt      string `json:"updated_at"`
}

{% endfor %}
