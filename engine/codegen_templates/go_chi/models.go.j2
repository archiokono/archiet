{# app/modules/solutions_product/templates/go_chi/models.go.j2 #}
{# Generates Go structs from confirmed architecture field definitions. #}
{# Context: solution_id, bundle_id, models (dict of model_name -> model_def with fields/enums) #}
// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}
// DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

package main

import (
	"time"

	"github.com/google/uuid"
)

{% for model_name, model_def in models.items() %}
{% if model_def.enums %}
{% for enum_name, enum_values in model_def.enums.items() %}
// {{ enum_name }} represents valid values for {{ model_name }}.
type {{ enum_name }} string

const (
{% for val in enum_values %}
	{{ enum_name }}{{ val | replace('-', '') | replace(' ', '') | capitalize }} {{ enum_name }} = "{{ val }}"
{% endfor %}
)

{% endfor %}
{% endif %}
// {{ model_name }} — {{ model_def.description or model_name + ' entity.' }}
type {{ model_name }} struct {
{% for field in model_def.fields %}
{% if field.type == 'uuid' or field.format == 'uuid' %}
	{{ field.name | capitalize }} uuid.UUID `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'string' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required{% if field.max_length is defined %},max={{ field.max_length }}{% endif %}"{% endif %}`
{% elif field.type == 'integer' %}
	{{ field.name | capitalize }} int64 `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'number' or field.type == 'decimal' %}
	{{ field.name | capitalize }} float64 `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'boolean' %}
	{{ field.name | capitalize }} bool `json:"{{ field.name }}" db:"{{ field.name }}"`
{% elif field.type == 'timestamp' or field.format == 'date-time' %}
	{{ field.name | capitalize }} time.Time `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} {{ model_name }}{{ field.name | capitalize }}Enum `json:"{{ field.name }}" db:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }}" db:"{{ field.name }}"`
{% else %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}" db:"{{ field.name }}"`
{% endif %}
{% endfor %}
}

// {{ model_name }}Create — request payload for creating a {{ model_name }}.
type {{ model_name }}Create struct {
{% for field in model_def.fields if not field.readonly %}
{% if field.type == 'uuid' or field.format == 'uuid' %}
	{{ field.name | capitalize }} uuid.UUID `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'string' %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"{% if field.required %} validate:"required{% if field.max_length is defined %},max={{ field.max_length }}{% endif %}"{% endif %}`
{% elif field.type == 'integer' %}
	{{ field.name | capitalize }} int64 `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'number' or field.type == 'decimal' %}
	{{ field.name | capitalize }} float64 `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'boolean' %}
	{{ field.name | capitalize }} bool `json:"{{ field.name }}"`
{% elif field.type == 'timestamp' or field.format == 'date-time' %}
	{{ field.name | capitalize }} time.Time `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} {{ model_name }}{{ field.name | capitalize }}Enum `json:"{{ field.name }}"{% if field.required %} validate:"required"{% endif %}`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }}"`
{% else %}
	{{ field.name | capitalize }} string `json:"{{ field.name }}"`
{% endif %}
{% endfor %}
}

// {{ model_name }}Update — request payload for updating a {{ model_name }}.
type {{ model_name }}Update struct {
{% for field in model_def.fields if not field.readonly %}
{% if field.type == 'uuid' or field.format == 'uuid' %}
	{{ field.name | capitalize }} *uuid.UUID `json:"{{ field.name }},omitempty"`
{% elif field.type == 'string' %}
	{{ field.name | capitalize }} *string `json:"{{ field.name }},omitempty"`
{% elif field.type == 'integer' %}
	{{ field.name | capitalize }} *int64 `json:"{{ field.name }},omitempty"`
{% elif field.type == 'number' or field.type == 'decimal' %}
	{{ field.name | capitalize }} *float64 `json:"{{ field.name }},omitempty"`
{% elif field.type == 'boolean' %}
	{{ field.name | capitalize }} *bool `json:"{{ field.name }},omitempty"`
{% elif field.type == 'timestamp' or field.format == 'date-time' %}
	{{ field.name | capitalize }} *time.Time `json:"{{ field.name }},omitempty"`
{% elif field.type == 'enum' %}
	{{ field.name | capitalize }} *{{ model_name }}{{ field.name | capitalize }}Enum `json:"{{ field.name }},omitempty"`
{% elif field.type == 'json' %}
	{{ field.name | capitalize }} map[string]interface{} `json:"{{ field.name }},omitempty"`
{% else %}
	{{ field.name | capitalize }} *string `json:"{{ field.name }},omitempty"`
{% endif %}
{% endfor %}
}

{% endfor %}
