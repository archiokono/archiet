
# GENERATED BY ARCHIE — entity: analyticsengine
# solution_id: 5373
# bundle_id: code_2026-04-06T18-28-31Z
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

from __future__ import annotations

import uuid
import logging
from http import HTTPStatus

from flask import Blueprint, jsonify, request
from flask_jwt_extended import jwt_required, get_jwt_identity
from marshmallow import ValidationError
from sqlalchemy import or_

from database import db
from app.models.analyticsengine import Analyticsengine
from app.schemas.analyticsengine_schema import (
    AnalyticsengineSchema,
    AnalyticsengineCreateSchema,
    AnalyticsengineUpdateSchema,
)

logger = logging.getLogger(__name__)

analyticsengine_bp = Blueprint(
    "analyticsengine",
    __name__,
)

_schema = AnalyticsengineSchema()
_schema_many = AnalyticsengineSchema(many=True)
_create_schema = AnalyticsengineCreateSchema()
_update_schema = AnalyticsengineUpdateSchema()

# ---------------------------------------------------------------------------
# GET /  — list with pagination + optional search
# ---------------------------------------------------------------------------

@analyticsengine_bp.route("/", methods=["GET"])
@jwt_required()
def list_analyticsengines():
    """Return a paginated list of analyticsengine records."""
    page = request.args.get("page", 1, type=int)
    per_page = min(request.args.get("per_page", 20, type=int), 100)
    search = request.args.get("search", "", type=str).strip()

    query = Analyticsengine.query

    paginated = query.order_by(Analyticsengine.created_at.desc()).paginate(
        page=page, per_page=per_page, error_out=False
    )
    return jsonify(
        {
            "items": _schema_many.dump(paginated.items),
            "total": paginated.total,
            "page": paginated.page,
            "pages": paginated.pages,
            "per_page": paginated.per_page,
        }
    )

# ---------------------------------------------------------------------------
# GET /<id>  — single record
# ---------------------------------------------------------------------------

@analyticsengine_bp.route("/<string:item_id>", methods=["GET"])
@jwt_required()
def get_analyticsengine(item_id: str):
    item = db.session.get(Analyticsengine, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# POST /  — create
# ---------------------------------------------------------------------------

@analyticsengine_bp.route("/", methods=["POST"])
@jwt_required()
def create_analyticsengine():
    try:
        data = _create_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    item = Analyticsengine(
        id=str(uuid.uuid4()),

        **{k: v for k, v in data.items() if v is not None},
    )
    db.session.add(item)
    db.session.commit()
    logger.info("Created analyticsengine id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item)), HTTPStatus.CREATED

# ---------------------------------------------------------------------------
# PUT /<id>  — full update
# ---------------------------------------------------------------------------

@analyticsengine_bp.route("/<string:item_id>", methods=["PUT"])
@jwt_required()
def update_analyticsengine(item_id: str):
    item = db.session.get(Analyticsengine, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND

    try:
        data = _update_schema.load(request.get_json(force=True) or {})
    except ValidationError as exc:
        return jsonify({"errors": exc.messages}), HTTPStatus.UNPROCESSABLE_ENTITY

    for key, value in data.items():
        if value is not None:
            setattr(item, key, value)

    db.session.commit()
    logger.info("Updated analyticsengine id=%s user=%s", item.id, get_jwt_identity())
    return jsonify(_schema.dump(item))

# ---------------------------------------------------------------------------
# DELETE /<id>
# ---------------------------------------------------------------------------

@analyticsengine_bp.route("/<string:item_id>", methods=["DELETE"])
@jwt_required()
def delete_analyticsengine(item_id: str):
    item = db.session.get(Analyticsengine, item_id)
    if item is None:
        return jsonify({"error": "not found"}), HTTPStatus.NOT_FOUND
    db.session.delete(item)
    db.session.commit()
    logger.info("Deleted analyticsengine id=%s user=%s", item_id, get_jwt_identity())
    return "", HTTPStatus.NO_CONTENT
