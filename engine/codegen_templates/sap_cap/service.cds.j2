{# service.cds.j2 — CDS service definition per entity.
   Context: solution_id, bundle_id, namespace, entity_name, service_name, service_kebab,
            read_scope, write_scope #}
// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}

using { {{ namespace }}.{{ entity_name }} } from '../db/schema';

/**
 * {{ service_name }} — OData V4 service
 */
@requires: '{{ read_scope }}'
service {{ service_name }} @(path: '/{{ service_kebab }}') {
  @requires: '{{ write_scope }}'
  entity {{ entity_name }} as projection on {{ namespace }}.{{ entity_name }};
}
