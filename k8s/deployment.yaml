


# GENERATED BY ARCHIE
# solution_id: 5373
# bundle_id: code_2026-04-06T18-28-31Z
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: archiet-—-ai-native-architecture-to-code-saas-platform
  labels:
    app: archiet-—-ai-native-architecture-to-code-saas-platform
    archie-solution: "5373"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: archiet-—-ai-native-architecture-to-code-saas-platform
  template:
    metadata:
      labels:
        app: archiet-—-ai-native-architecture-to-code-saas-platform
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: archiet-—-ai-native-architecture-to-code-saas-platform
          image: archiet-—-ai-native-architecture-to-code-saas-platform:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: archiet-—-ai-native-architecture-to-code-saas-platform-secrets
                  key: database-url
            
            
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: archiet-—-ai-native-architecture-to-code-saas-platform-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: archiet-—-ai-native-architecture-to-code-saas-platform
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: v1
kind: Service
metadata:
  name: archiet-—-ai-native-architecture-to-code-saas-platform
spec:
  selector:
    app: archiet-—-ai-native-architecture-to-code-saas-platform
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP

