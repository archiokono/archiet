# CLAUDE.md — Archiet — AI-Native Architecture-to-Code SaaS Platform

> **Read this file completely before making any changes.**
> The sections are ordered by importance: Hard Rules first, then context.

## HARD RULES — Non-Negotiable Constraints

> These are architectural constraints derived from the ArchiMate model.
> Violating any one of these produces INCORRECT software, not just bad style.
> See `PRINCIPLES.md` for full details and enforcement locations.

- **✅ MUST**: API versioning strategy
- **✅ MUST**: Data residency

## Architecture

- **Framework**: python-flask
- **Database**: SQLAlchemy (sync) + Flask-Migrate
- **Auth**: JWT local
- **Genome version**: 1.0.0
- **Generated by**: ARCHIE (spec hash: 81750cb319c0fa45)

## Entities

### SmtpRelayGcp
- **Table**: `smtp_relay_gcps`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### PrimaryDataStore
- **Table**: `primary_data_stores`
- **Fields**: `name`: string (required), `description`: text, `storage_type`: string (required), `capacity`: string (required), `connection_string`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### IdentityAndAccessManagement
- **Table**: `identity_and_access_managements`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### AuthenticationService
- **Table**: `authentication_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### MonitoringPlatform
- **Table**: `monitoring_platforms`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### AnalyticsEngine
- **Table**: `analytics_engines`
- **Fields**: `name`: string (required), `description`: text, `metric_name`: string (required), `value`: decimal (required), `target`: decimal (required), `unit`: string (required), `measured_at`: datetime (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### ReportingService
- **Table**: `reporting_services`
- **Fields**: `name`: string (required), `description`: text, `title`: string (required), `document_type`: string (required), `effective_date`: datetime (required), `expiry_date`: datetime (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GcpCohesityForSystemsBackup
- **Table**: `gcp_cohesity_for_systems_backups`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### ApiGateway
- **Table**: `api_gateways`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### MessageBroker
- **Table**: `message_brokers`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### BusinessLogicLayer
- **Table**: `business_logic_layers`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CoreBusinessFunction
- **Table**: `core_business_functions`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CoreBusinessApi
- **Table**: `core_business_apis`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### FrontendApplication
- **Table**: `frontend_applications`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### ActiveDirectoryAd
- **Table**: `active_directory_ads`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### NotificationService
- **Table**: `notification_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GoogleGeminiModels
- **Table**: `google_gemini_models`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### VeritasNetbackupSaaS
- **Table**: `veritas_netbackup_saa_s`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### BlueprintJourneyInterface
- **Table**: `blueprint_journey_interfaces`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### ArchitectureTemplateGallery
- **Table**: `architecture_template_galleries`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### BlueprintPublicViewer
- **Table**: `blueprint_public_viewers`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CicdToolchain
- **Table**: `cicd_toolchains`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CicdPipeline
- **Table**: `cicd_pipelines`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### HealthMonitoring
- **Table**: `health_monitorings`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GenomeSpecGenerator
- **Table**: `genome_spec_generators`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CodeGenerationService
- **Table**: `code_generation_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### ExportService
- **Table**: `export_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### JourneySessionOrchestrator
- **Table**: `journey_session_orchestrators`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### StripeWebhookHandler
- **Table**: `stripe_webhook_handlers`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### RowLevelSecurityEnforcer
- **Table**: `row_level_security_enforcers`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GdprComplianceService
- **Table**: `gdpr_compliance_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### AiArchitectureEngine
- **Table**: `ai_architecture_engines`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### UsageMeteringLimiting
- **Table**: `usage_metering_limitings`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### QualityScoringEngine
- **Table**: `quality_scoring_engines`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GitHubIntegrationService
- **Table**: `git_hub_integration_services`
- **Fields**: `name`: string (required), `description`: text, `endpoint_url`: string (required), `service_type`: string (required), `version`: string (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### CoreBusiness
- **Table**: `core_businesses`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### KeyMetricsAndKpIs
- **Table**: `key_metrics_and_kp_es`
- **Fields**: `name`: string (required), `description`: text, `metric_name`: string (required), `value`: decimal (required), `target`: decimal (required), `unit`: string (required), `measured_at`: datetime (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### AnalyticsDataStore
- **Table**: `analytics_data_stores`
- **Fields**: `name`: string (required), `description`: text, `metric_name`: string (required), `value`: decimal (required), `target`: decimal (required), `unit`: string (required), `measured_at`: datetime (required), `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### Workspace
- **Table**: `workspaces`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### JourneySessionConversations
- **Table**: `journey_session_conversations`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GenomeSpecDefinitions
- **Table**: `genome_spec_definitions`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

### GeneratedCodeArtifacts
- **Table**: `generated_code_artifacts`
- **Fields**: `name`: string (required), `description`: text, `status`: enum (required), `created_at`: datetime, `updated_at`: datetime

## API Endpoints

### SmtpRelayGcp
- `GET /api/smtp_relay_gcps` — List SmtpRelayGcps
- `POST /api/smtp_relay_gcps` — Create SmtpRelayGcp
- `GET /api/smtp_relay_gcps/{id}` — Get SmtpRelayGcp by ID
- `PUT /api/smtp_relay_gcps/{id}` — Update SmtpRelayGcp
- `DELETE /api/smtp_relay_gcps/{id}` — Delete SmtpRelayGcp

### PrimaryDataStore
- `GET /api/primary_data_stores` — List PrimaryDataStores
- `POST /api/primary_data_stores` — Create PrimaryDataStore
- `GET /api/primary_data_stores/{id}` — Get PrimaryDataStore by ID
- `PUT /api/primary_data_stores/{id}` — Update PrimaryDataStore
- `DELETE /api/primary_data_stores/{id}` — Delete PrimaryDataStore

### IdentityAndAccessManagement
- `GET /api/identity_and_access_managements` — List IdentityAndAccessManagements
- `POST /api/identity_and_access_managements` — Create IdentityAndAccessManagement
- `GET /api/identity_and_access_managements/{id}` — Get IdentityAndAccessManagement by ID
- `PUT /api/identity_and_access_managements/{id}` — Update IdentityAndAccessManagement
- `DELETE /api/identity_and_access_managements/{id}` — Delete IdentityAndAccessManagement

### AuthenticationService
- `GET /api/authentication_services` — List AuthenticationServices
- `POST /api/authentication_services` — Create AuthenticationService
- `GET /api/authentication_services/{id}` — Get AuthenticationService by ID
- `PUT /api/authentication_services/{id}` — Update AuthenticationService
- `DELETE /api/authentication_services/{id}` — Delete AuthenticationService

### MonitoringPlatform
- `GET /api/monitoring_platforms` — List MonitoringPlatforms
- `POST /api/monitoring_platforms` — Create MonitoringPlatform
- `GET /api/monitoring_platforms/{id}` — Get MonitoringPlatform by ID
- `PUT /api/monitoring_platforms/{id}` — Update MonitoringPlatform
- `DELETE /api/monitoring_platforms/{id}` — Delete MonitoringPlatform

### AnalyticsEngine
- `GET /api/analytics_engines` — List AnalyticsEngines
- `POST /api/analytics_engines` — Create AnalyticsEngine
- `GET /api/analytics_engines/{id}` — Get AnalyticsEngine by ID
- `PUT /api/analytics_engines/{id}` — Update AnalyticsEngine
- `DELETE /api/analytics_engines/{id}` — Delete AnalyticsEngine

### ReportingService
- `GET /api/reporting_services` — List ReportingServices
- `POST /api/reporting_services` — Create ReportingService
- `GET /api/reporting_services/{id}` — Get ReportingService by ID
- `PUT /api/reporting_services/{id}` — Update ReportingService
- `DELETE /api/reporting_services/{id}` — Delete ReportingService

### GcpCohesityForSystemsBackup
- `GET /api/gcp_cohesity_for_systems_backups` — List GcpCohesityForSystemsBackups
- `POST /api/gcp_cohesity_for_systems_backups` — Create GcpCohesityForSystemsBackup
- `GET /api/gcp_cohesity_for_systems_backups/{id}` — Get GcpCohesityForSystemsBackup by ID
- `PUT /api/gcp_cohesity_for_systems_backups/{id}` — Update GcpCohesityForSystemsBackup
- `DELETE /api/gcp_cohesity_for_systems_backups/{id}` — Delete GcpCohesityForSystemsBackup

### ApiGateway
- `GET /api/api_gateways` — List ApiGateways
- `POST /api/api_gateways` — Create ApiGateway
- `GET /api/api_gateways/{id}` — Get ApiGateway by ID
- `PUT /api/api_gateways/{id}` — Update ApiGateway
- `DELETE /api/api_gateways/{id}` — Delete ApiGateway

### MessageBroker
- `GET /api/message_brokers` — List MessageBrokers
- `POST /api/message_brokers` — Create MessageBroker
- `GET /api/message_brokers/{id}` — Get MessageBroker by ID
- `PUT /api/message_brokers/{id}` — Update MessageBroker
- `DELETE /api/message_brokers/{id}` — Delete MessageBroker

### BusinessLogicLayer
- `GET /api/business_logic_layers` — List BusinessLogicLayers
- `POST /api/business_logic_layers` — Create BusinessLogicLayer
- `GET /api/business_logic_layers/{id}` — Get BusinessLogicLayer by ID
- `PUT /api/business_logic_layers/{id}` — Update BusinessLogicLayer
- `DELETE /api/business_logic_layers/{id}` — Delete BusinessLogicLayer

### CoreBusinessFunction
- `GET /api/core_business_functions` — List CoreBusinessFunctions
- `POST /api/core_business_functions` — Create CoreBusinessFunction
- `GET /api/core_business_functions/{id}` — Get CoreBusinessFunction by ID
- `PUT /api/core_business_functions/{id}` — Update CoreBusinessFunction
- `DELETE /api/core_business_functions/{id}` — Delete CoreBusinessFunction

### CoreBusinessApi
- `GET /api/core_business_apis` — List CoreBusinessApis
- `POST /api/core_business_apis` — Create CoreBusinessApi
- `GET /api/core_business_apis/{id}` — Get CoreBusinessApi by ID
- `PUT /api/core_business_apis/{id}` — Update CoreBusinessApi
- `DELETE /api/core_business_apis/{id}` — Delete CoreBusinessApi

### FrontendApplication
- `GET /api/frontend_applications` — List FrontendApplications
- `POST /api/frontend_applications` — Create FrontendApplication
- `GET /api/frontend_applications/{id}` — Get FrontendApplication by ID
- `PUT /api/frontend_applications/{id}` — Update FrontendApplication
- `DELETE /api/frontend_applications/{id}` — Delete FrontendApplication

### ActiveDirectoryAd
- `GET /api/active_directory_ads` — List ActiveDirectoryAds
- `POST /api/active_directory_ads` — Create ActiveDirectoryAd
- `GET /api/active_directory_ads/{id}` — Get ActiveDirectoryAd by ID
- `PUT /api/active_directory_ads/{id}` — Update ActiveDirectoryAd
- `DELETE /api/active_directory_ads/{id}` — Delete ActiveDirectoryAd

### NotificationService
- `GET /api/notification_services` — List NotificationServices
- `POST /api/notification_services` — Create NotificationService
- `GET /api/notification_services/{id}` — Get NotificationService by ID
- `PUT /api/notification_services/{id}` — Update NotificationService
- `DELETE /api/notification_services/{id}` — Delete NotificationService

### GoogleGeminiModels
- `GET /api/google_gemini_models` — List GoogleGeminiModelss
- `POST /api/google_gemini_models` — Create GoogleGeminiModels
- `GET /api/google_gemini_models/{id}` — Get GoogleGeminiModels by ID
- `PUT /api/google_gemini_models/{id}` — Update GoogleGeminiModels
- `DELETE /api/google_gemini_models/{id}` — Delete GoogleGeminiModels

### VeritasNetbackupSaaS
- `GET /api/veritas_netbackup_saa_s` — List VeritasNetbackupSaaSs
- `POST /api/veritas_netbackup_saa_s` — Create VeritasNetbackupSaaS
- `GET /api/veritas_netbackup_saa_s/{id}` — Get VeritasNetbackupSaaS by ID
- `PUT /api/veritas_netbackup_saa_s/{id}` — Update VeritasNetbackupSaaS
- `DELETE /api/veritas_netbackup_saa_s/{id}` — Delete VeritasNetbackupSaaS

### BlueprintJourneyInterface
- `GET /api/blueprint_journey_interfaces` — List BlueprintJourneyInterfaces
- `POST /api/blueprint_journey_interfaces` — Create BlueprintJourneyInterface
- `GET /api/blueprint_journey_interfaces/{id}` — Get BlueprintJourneyInterface by ID
- `PUT /api/blueprint_journey_interfaces/{id}` — Update BlueprintJourneyInterface
- `DELETE /api/blueprint_journey_interfaces/{id}` — Delete BlueprintJourneyInterface

### ArchitectureTemplateGallery
- `GET /api/architecture_template_galleries` — List ArchitectureTemplateGallerys
- `POST /api/architecture_template_galleries` — Create ArchitectureTemplateGallery
- `GET /api/architecture_template_galleries/{id}` — Get ArchitectureTemplateGallery by ID
- `PUT /api/architecture_template_galleries/{id}` — Update ArchitectureTemplateGallery
- `DELETE /api/architecture_template_galleries/{id}` — Delete ArchitectureTemplateGallery

### BlueprintPublicViewer
- `GET /api/blueprint_public_viewers` — List BlueprintPublicViewers
- `POST /api/blueprint_public_viewers` — Create BlueprintPublicViewer
- `GET /api/blueprint_public_viewers/{id}` — Get BlueprintPublicViewer by ID
- `PUT /api/blueprint_public_viewers/{id}` — Update BlueprintPublicViewer
- `DELETE /api/blueprint_public_viewers/{id}` — Delete BlueprintPublicViewer

### CicdToolchain
- `GET /api/cicd_toolchains` — List CicdToolchains
- `POST /api/cicd_toolchains` — Create CicdToolchain
- `GET /api/cicd_toolchains/{id}` — Get CicdToolchain by ID
- `PUT /api/cicd_toolchains/{id}` — Update CicdToolchain
- `DELETE /api/cicd_toolchains/{id}` — Delete CicdToolchain

### CicdPipeline
- `GET /api/cicd_pipelines` — List CicdPipelines
- `POST /api/cicd_pipelines` — Create CicdPipeline
- `GET /api/cicd_pipelines/{id}` — Get CicdPipeline by ID
- `PUT /api/cicd_pipelines/{id}` — Update CicdPipeline
- `DELETE /api/cicd_pipelines/{id}` — Delete CicdPipeline

### HealthMonitoring
- `GET /api/health_monitorings` — List HealthMonitorings
- `POST /api/health_monitorings` — Create HealthMonitoring
- `GET /api/health_monitorings/{id}` — Get HealthMonitoring by ID
- `PUT /api/health_monitorings/{id}` — Update HealthMonitoring
- `DELETE /api/health_monitorings/{id}` — Delete HealthMonitoring

### GenomeSpecGenerator
- `GET /api/genome_spec_generators` — List GenomeSpecGenerators
- `POST /api/genome_spec_generators` — Create GenomeSpecGenerator
- `GET /api/genome_spec_generators/{id}` — Get GenomeSpecGenerator by ID
- `PUT /api/genome_spec_generators/{id}` — Update GenomeSpecGenerator
- `DELETE /api/genome_spec_generators/{id}` — Delete GenomeSpecGenerator

### CodeGenerationService
- `GET /api/code_generation_services` — List CodeGenerationServices
- `POST /api/code_generation_services` — Create CodeGenerationService
- `GET /api/code_generation_services/{id}` — Get CodeGenerationService by ID
- `PUT /api/code_generation_services/{id}` — Update CodeGenerationService
- `DELETE /api/code_generation_services/{id}` — Delete CodeGenerationService

### ExportService
- `GET /api/export_services` — List ExportServices
- `POST /api/export_services` — Create ExportService
- `GET /api/export_services/{id}` — Get ExportService by ID
- `PUT /api/export_services/{id}` — Update ExportService
- `DELETE /api/export_services/{id}` — Delete ExportService

### JourneySessionOrchestrator
- `GET /api/journey_session_orchestrators` — List JourneySessionOrchestrators
- `POST /api/journey_session_orchestrators` — Create JourneySessionOrchestrator
- `GET /api/journey_session_orchestrators/{id}` — Get JourneySessionOrchestrator by ID
- `PUT /api/journey_session_orchestrators/{id}` — Update JourneySessionOrchestrator
- `DELETE /api/journey_session_orchestrators/{id}` — Delete JourneySessionOrchestrator

### StripeWebhookHandler
- `GET /api/stripe_webhook_handlers` — List StripeWebhookHandlers
- `POST /api/stripe_webhook_handlers` — Create StripeWebhookHandler
- `GET /api/stripe_webhook_handlers/{id}` — Get StripeWebhookHandler by ID
- `PUT /api/stripe_webhook_handlers/{id}` — Update StripeWebhookHandler
- `DELETE /api/stripe_webhook_handlers/{id}` — Delete StripeWebhookHandler

### RowLevelSecurityEnforcer
- `GET /api/row_level_security_enforcers` — List RowLevelSecurityEnforcers
- `POST /api/row_level_security_enforcers` — Create RowLevelSecurityEnforcer
- `GET /api/row_level_security_enforcers/{id}` — Get RowLevelSecurityEnforcer by ID
- `PUT /api/row_level_security_enforcers/{id}` — Update RowLevelSecurityEnforcer
- `DELETE /api/row_level_security_enforcers/{id}` — Delete RowLevelSecurityEnforcer

### GdprComplianceService
- `GET /api/gdpr_compliance_services` — List GdprComplianceServices
- `POST /api/gdpr_compliance_services` — Create GdprComplianceService
- `GET /api/gdpr_compliance_services/{id}` — Get GdprComplianceService by ID
- `PUT /api/gdpr_compliance_services/{id}` — Update GdprComplianceService
- `DELETE /api/gdpr_compliance_services/{id}` — Delete GdprComplianceService

### AiArchitectureEngine
- `GET /api/ai_architecture_engines` — List AiArchitectureEngines
- `POST /api/ai_architecture_engines` — Create AiArchitectureEngine
- `GET /api/ai_architecture_engines/{id}` — Get AiArchitectureEngine by ID
- `PUT /api/ai_architecture_engines/{id}` — Update AiArchitectureEngine
- `DELETE /api/ai_architecture_engines/{id}` — Delete AiArchitectureEngine

### UsageMeteringLimiting
- `GET /api/usage_metering_limitings` — List UsageMeteringLimitings
- `POST /api/usage_metering_limitings` — Create UsageMeteringLimiting
- `GET /api/usage_metering_limitings/{id}` — Get UsageMeteringLimiting by ID
- `PUT /api/usage_metering_limitings/{id}` — Update UsageMeteringLimiting
- `DELETE /api/usage_metering_limitings/{id}` — Delete UsageMeteringLimiting

### QualityScoringEngine
- `GET /api/quality_scoring_engines` — List QualityScoringEngines
- `POST /api/quality_scoring_engines` — Create QualityScoringEngine
- `GET /api/quality_scoring_engines/{id}` — Get QualityScoringEngine by ID
- `PUT /api/quality_scoring_engines/{id}` — Update QualityScoringEngine
- `DELETE /api/quality_scoring_engines/{id}` — Delete QualityScoringEngine

### GitHubIntegrationService
- `GET /api/git_hub_integration_services` — List GitHubIntegrationServices
- `POST /api/git_hub_integration_services` — Create GitHubIntegrationService
- `GET /api/git_hub_integration_services/{id}` — Get GitHubIntegrationService by ID
- `PUT /api/git_hub_integration_services/{id}` — Update GitHubIntegrationService
- `DELETE /api/git_hub_integration_services/{id}` — Delete GitHubIntegrationService

### CoreBusiness
- `GET /api/core_businesses` — List CoreBusinessEntitys
- `POST /api/core_businesses` — Create CoreBusinessEntity
- `GET /api/core_businesses/{id}` — Get CoreBusinessEntity by ID
- `PUT /api/core_businesses/{id}` — Update CoreBusinessEntity
- `DELETE /api/core_businesses/{id}` — Delete CoreBusinessEntity

### KeyMetricsAndKpIs
- `GET /api/key_metrics_and_kp_es` — List KeyMetricsAndKpIss
- `POST /api/key_metrics_and_kp_es` — Create KeyMetricsAndKpIs
- `GET /api/key_metrics_and_kp_es/{id}` — Get KeyMetricsAndKpIs by ID
- `PUT /api/key_metrics_and_kp_es/{id}` — Update KeyMetricsAndKpIs
- `DELETE /api/key_metrics_and_kp_es/{id}` — Delete KeyMetricsAndKpIs

### AnalyticsDataStore
- `GET /api/analytics_data_stores` — List AnalyticsDataStores
- `POST /api/analytics_data_stores` — Create AnalyticsDataStore
- `GET /api/analytics_data_stores/{id}` — Get AnalyticsDataStore by ID
- `PUT /api/analytics_data_stores/{id}` — Update AnalyticsDataStore
- `DELETE /api/analytics_data_stores/{id}` — Delete AnalyticsDataStore

### Workspace
- `GET /api/workspaces` — List WorkspaceDatas
- `POST /api/workspaces` — Create WorkspaceData
- `GET /api/workspaces/{id}` — Get WorkspaceData by ID
- `PUT /api/workspaces/{id}` — Update WorkspaceData
- `DELETE /api/workspaces/{id}` — Delete WorkspaceData

### JourneySessionConversations
- `GET /api/journey_session_conversations` — List JourneySessionConversationss
- `POST /api/journey_session_conversations` — Create JourneySessionConversations
- `GET /api/journey_session_conversations/{id}` — Get JourneySessionConversations by ID
- `PUT /api/journey_session_conversations/{id}` — Update JourneySessionConversations
- `DELETE /api/journey_session_conversations/{id}` — Delete JourneySessionConversations

### GenomeSpecDefinitions
- `GET /api/genome_spec_definitions` — List GenomeSpecDefinitionss
- `POST /api/genome_spec_definitions` — Create GenomeSpecDefinitions
- `GET /api/genome_spec_definitions/{id}` — Get GenomeSpecDefinitions by ID
- `PUT /api/genome_spec_definitions/{id}` — Update GenomeSpecDefinitions
- `DELETE /api/genome_spec_definitions/{id}` — Delete GenomeSpecDefinitions

### GeneratedCodeArtifacts
- `GET /api/generated_code_artifacts` — List GeneratedCodeArtifactss
- `POST /api/generated_code_artifacts` — Create GeneratedCodeArtifacts
- `GET /api/generated_code_artifacts/{id}` — Get GeneratedCodeArtifacts by ID
- `PUT /api/generated_code_artifacts/{id}` — Update GeneratedCodeArtifacts
- `DELETE /api/generated_code_artifacts/{id}` — Delete GeneratedCodeArtifacts

## File Structure

```
app/
  main.py              # FastAPI entry point + middleware
  database.py          # Async SQLAlchemy engine + session
  models/              # SQLAlchemy ORM models (one per entity)
  schemas/             # Pydantic request/response schemas
  routes/              # FastAPI routers (one per service)
tests/                 # Contract + behavioral + security tests
ARCHITECTURE.md        # Full architecture context + traceability
CLAUDE.md              # This file
openapi.yaml           # OpenAPI 3.1 spec (authoritative)
docker-compose.yml     # Local dev orchestration
Dockerfile             # Multi-stage production build
```

## Development

```bash
docker-compose up --build  # starts app + database
pytest tests/ -v           # run all tests
alembic upgrade head       # apply migrations
```

## Rules for AI Agents

1. **Read HARD RULES above before touching any file.**
2. **Read `PRINCIPLES.md`** — architectural principles with code implications and enforcement files.
3. **Read `DOMAIN.md`** — domain vocabulary and ubiquitous language. Never rename domain terms.
4. **Read `DEPENDENCIES.md`** — before changing any interface, check the impact matrix.
5. **Read `ARCHITECTURE.md`** — full ArchiMate traceability + acceptance criteria.
6. **Run tests before and after changes.** `pytest tests/ -v` must pass.
7. **API contracts in `openapi.yaml` are authoritative.** Routes must match the spec.
8. **Business rules in `handlers/` are architecture-enforced.** Don't remove or weaken them.
9. **Auth: JWT local.** Don't bypass `Depends(get_current_user)` on any protected route.
10. **Every model field change needs an Alembic migration.** No bare `db.create_all()`.

## Key Architecture Principles (summary)

- **Least privilege access**: Grant minimum permissions required for each role
- **Defense in depth**: Multiple independent security layers
- **Infrastructure as code**: All infrastructure defined declaratively

## Regeneration

This code was generated from an Architectural Genome. To regenerate:
1. Edit `architectural_genome.yaml` to change the architecture
2. Run: `POST /solutions/5373/codegen/generate {"generation_mode": "genome"}`
3. Modified files will be detected and warned before overwrite (drift detection)

---
*Generated by A.R.C.H.I.E. for solution 5373 (spec hash: 81750cb319c0fa45)*
