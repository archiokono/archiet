# Google Play Console Metadata — {{ solution_name }}

Generated by A.R.C.H.I.E. Fill in the bracketed fields before submission.

---

## App Details

| Field | Value | Limit |
|---|---|---|
| **App name** | {{ solution_name | truncate(30, true, '') }} | 30 chars |
| **Package name** | {{ bundle_id | replace('-', '_') }} | — |
| **Category** | Business | — |
| **Tags** | enterprise, business, productivity | — |
| **Content rating** | Everyone | — |

---

## Short Description (80 chars max)

{{ (solution_name + ' — manage ' + (classes | map(attribute='name') | map('lower') | map('replace', '_', ' ') | join(', ') | truncate(40, true, ''))) | truncate(80, true, '') }}

---

## Full Description (4000 chars max)

{{ solution_name }} is an enterprise application for managing:

{% for cls in classes %}
• {{ cls.name | replace('_', ' ') | title }}: {{ cls.description | default('Track and manage ' + (cls.name | replace('_', ' ') | lower) + ' records.') }}
{% endfor %}

KEY FEATURES:
• Real-time sync with your {{ solution_name }} backend
• Works offline — queues changes and syncs when reconnected
{% if has_biometric %}• Biometric login (fingerprint / face unlock){% endif %}
{% if push_enabled %}• Push notifications for real-time updates{% endif %}
• Full CRUD with server-side validation
• Secure JWT authentication via encrypted storage

REQUIREMENTS:
• Android 8.0 (API level 26) or higher
• Connection to {{ solution_name }} API: {{ api_base_url }}

[Add your company / support contact info here.]

---

## Privacy Policy URL (required)

{{ privacy_policy_url }}

---

## Screenshots Required

See `SCREENSHOTS.md` for exact specifications.

---

## Data Safety (complete in Play Console)

| Data type | Collected | Shared | Required |
|---|---|---|---|
{% if auth == "jwt-local" %}
| Email address | Yes | No | Yes |
| User ID | Yes | No | Yes |
{% endif %}
| App activity | Yes | No | No |

Encryption in transit: Yes (HTTPS)
User can request deletion: [specify your deletion process]
