{# go_chi/dto/pagination.go.j2 #}
{# PaginatedResponse generic struct. #}
{# Context: solution_id, bundle_id #}
// GENERATED BY ARCHIE
// solution_id: {{ solution_id }}
// bundle_id: {{ bundle_id }}
// DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE

package dto

// PaginatedResponse is a generic paginated response wrapper.
type PaginatedResponse[T any] struct {
	Items   []T  `json:"items"`
	Total   int64 `json:"total"`
	Page    int   `json:"page"`
	PerPage int   `json:"per_page"`
	Pages   int   `json:"pages"`
	HasNext bool  `json:"has_next"`
}

// NewPaginatedResponse creates a PaginatedResponse from query results.
func NewPaginatedResponse[T any](items []T, total int64, page, perPage int) PaginatedResponse[T] {
	pages := int(total) / perPage
	if int(total)%perPage > 0 {
		pages++
	}
	return PaginatedResponse[T]{
		Items:   items,
		Total:   total,
		Page:    page,
		PerPage: perPage,
		Pages:   pages,
		HasNext: page < pages,
	}
}
