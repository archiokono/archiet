{# app/modules/solutions_product/templates/shared/istio_peer_auth.yaml.j2 #}
{# Istio mTLS PeerAuthentication policy. #}
{# Context: solution_id, bundle_id, solution_name #}
# GENERATED BY ARCHIE
# solution_id: {{ solution_id }}
# bundle_id: {{ bundle_id }}
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE
---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: {{ solution_name | lower | replace(' ', '-') }}-mtls
  labels:
    app: {{ solution_name | lower | replace(' ', '-') }}
    generated-by: archie
spec:
  selector:
    matchLabels:
      app: {{ solution_name | lower | replace(' ', '-') }}
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ solution_name | lower | replace(' ', '-') }}-authz
  labels:
    app: {{ solution_name | lower | replace(' ', '-') }}
    generated-by: archie
spec:
  selector:
    matchLabels:
      app: {{ solution_name | lower | replace(' ', '-') }}
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/*/sa/*"
      to:
        - operation:
            methods: ["GET", "POST", "PUT", "DELETE", "PATCH"]
            paths: ["/*"]
