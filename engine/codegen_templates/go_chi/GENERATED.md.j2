{# app/modules/solutions_product/templates/go_chi/GENERATED.md.j2 #}
{# Traceability doc linking back to ArchiMate elements. #}
{# Context: solution_id, bundle_id, solution_name, spec_hash, generated_at, maturity_score #}
# Generated by A.R.C.H.I.E.
- Solution: {{ solution_name }} (ID: {{ solution_id }})
- Bundle: {{ bundle_id }}
- Spec Hash: {{ spec_hash }}
- Generated: {{ generated_at }}
- Language: Go (Chi)
- Spec Maturity: {{ "%.0f" | format(maturity_score * 100) }}%

## Traceability
Every generated file traces back to the ArchiMate architecture via
`x-archimate-source` comments. Regenerate from ARCHIE to update.

## What's Implemented
- API route stubs using chi router (all return 501 -- business logic is Wave 2)
- Go structs with JSON tags and validation tags from solution schemas
- Contract tests using httptest validating request/response shapes
- JWT authentication middleware with role-based access control
- Structured logging with zerolog and request correlation IDs
- Multi-stage Docker build (distroless production image)
- Kubernetes manifests with security contexts and auto-scaling
- Docker Compose with PostgreSQL and Redis

## What's NOT Implemented
- Business logic (requires LLM-assisted generation -- Wave 2)
- Database connection pooling configuration
- OpenTelemetry tracing integration
- CI/CD pipeline (Wave 3)

## Running Locally
```bash
# Start all services
docker compose up -d

# Run tests
go test -v ./...

# Build binary
go build -o server .
./server
```
