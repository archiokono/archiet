


# GENERATED BY ARCHIE
# solution_id: 5373
# bundle_id: code_2026-04-06T18-28-31Z
# DO NOT EDIT IN PLACE -- REGENERATE FROM ARCHITECTURE








name: "archiet-—-ai-native-architecture-to-code-saas-platform CI/CD"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: archiet-—-ai-native-architecture-to-code-saas-platform

  REGISTRY: ghcr.io/${{ github.repository_owner }}


jobs:
  # ── Stage 1: Build & Lint ──
  build-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff

      - name: Run linter
        run: ruff check . --output-format=github

  # ── Stage 2: Test ──
  test:
    runs-on: ubuntu-latest
    needs: build-lint
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://test:test@localhost:5432/test_db  # secrets-safety-ok
      SECRET_KEY: ci-test-secret-key-not-for-production-use  # secrets-safety-ok
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio httpx

      - name: Run database migrations
        run: alembic upgrade head

      - name: Run unit tests with coverage
        run: pytest --cov=app --cov-report=xml --cov-report=term -q

      - name: Run contract tests
        run: pytest tests/contract/test_api_contract.py -v

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml


  # ── Stage 3: Security Scan ──
  security-scan:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t ${{ env.IMAGE_NAME }}:scan .

      - name: Run Trivy container scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}:scan
          format: table
          exit-code: 1
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run dependency audit
        run: |
          pip install pip-audit
          pip-audit -r requirements.txt


  # ── Stage 4: Push Image ──
  push-image:
    runs-on: ubuntu-latest

    needs: security-scan

    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4


      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}


      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

  # ── Stage 5: Deploy to Staging ──
  deploy-staging:
    runs-on: ubuntu-latest
    needs: push-image
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Configure kubectl
        uses: azure/setup-kubectl@v4

      - name: Set kubeconfig
        run: echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config

      - name: Update image tag in manifests
        run: |
          sed -i "s|image:.*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g" k8s/deployment.yaml

      - name: Apply K8s manifests to staging
        run: kubectl apply -f k8s/ -n staging

      - name: Wait for rollout
        run: kubectl rollout status deployment/archiet-—-ai-native-architecture-to-code-saas-platform -n staging --timeout=300s

      - name: Run smoke test
        run: |
          STAGING_URL=$(kubectl get svc archiet-—-ai-native-architecture-to-code-saas-platform -n staging -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          curl -sf "http://${STAGING_URL}/health" || exit 1

  # ── Stage 6: Deploy to Production ──
  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging

    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Configure kubectl
        uses: azure/setup-kubectl@v4

      - name: Set kubeconfig
        run: echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config

      - name: Update image tag in manifests
        run: |
          sed -i "s|image:.*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g" k8s/deployment.yaml

      - name: Apply K8s manifests to production
        run: kubectl apply -f k8s/ -n production

      - name: Wait for rollout
        run: kubectl rollout status deployment/archiet-—-ai-native-architecture-to-code-saas-platform -n production --timeout=300s

